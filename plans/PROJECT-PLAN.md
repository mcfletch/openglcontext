# OpenGLContext Project Plan

## Summary

**Status legend:** ✅ **Complete** — shipped, nothing required outstanding · 🟡 **Partial** — core landed but named pieces still missing · 📋 **Planned** — has a plan document, not yet built · ⬜ **Todo** — wanted, no plan document yet · 🛑 **Shelved** — deliberately deferred.

Rows are ordered active first, then completed work; long entries link to a **Details** section below.

| Plan | Status | Summary |
|------|--------|---------|
| [GLISTEEL-WORLD-AUTHORING.md](GLISTEEL-WORLD-AUTHORING.md) | 🟡 Partial | Large-world authoring for a racing game (`glisteel`) and its track editor (`glisteel-editor`): an owned glTF + 3D Tiles **writer**/octree baker, georeferenced multi-block DEM + procedural control maps, **roads** (highway/causeway/bridge/tunnel ops + reflections), **water** (rivers/lakes/whitewater/beaches), road-aware LOD refinement, and an editor toolkit (tool-modes, menus, surface picking, gizmos, ortho map). Authoring core in a new `OpenGLContext-editor` sibling; runtime render nodes in-engine; 60 fps on a discrete GPU. **§A is done:** the glTF writer is in `loaders/gltf/writer.py` and the baker is in the sibling; `oglc-bake` produces a world `oglc-view` streams, and it is equivalent-or-better against the engine's own terrain baker. §B–§I are ahead, tracked in [GLISTEEL-SLICE-A-HANDOFF.md](GLISTEEL-SLICE-A-HANDOFF.md). |
| [HANDOFF-FOREST-PERFORMANCE-2026-08-12.md](HANDOFF-FOREST-PERFORMANCE-2026-08-12.md) | 🟡 Partial | Hold 60 fps in `oglc-forest` on an Intel UHD 630. Tier 1 shipped (quality presets + measured-fps `auto` picker, off-thread stream recompute); Tier 2 engine work (finish glGet-state removal, indirect draws, persistent instance buffers) not started. ([details](#handoff-forest-performance-2026-08-12)) |
| [CODE-REVIEW-2026-08-03.md](CODE-REVIEW-2026-08-03.md) | 🟡 Partial | Final review before **3.0.0a1**: 20 findings, 19 fixed Red/Green (see the document for the table and remediations). **Remaining:** deploy the regenerated PyDoc pages (m1 code landed), and minors m11 (`README.md` lists `tkevents`/`fxevents` as removed while both remain) and m12 (orphaned `bin/profile_view.py`). Bundled font (m4) closed here; its successor is [FONT-ATLAS.md](FONT-ATLAS.md). |
| [FONT-ATLAS.md](FONT-ATLAS.md) | 📋 Planned | A paged glyph store to replace the ASCII-only, monospace shader-text atlas: a proportional sans-serif default with Latin-1 resident and on-demand Unicode pages (pre-baked or rasterized on the fly from a dropped-in TrueType source), an overridable `fontDirectories` search path with `default`/`default-mono` bundled last, and per-font licence attribution. **Plan only — awaiting review before implementation.** |
| [CODEBASE-CONSOLIDATION.md](CODEBASE-CONSOLIDATION.md) | 🟡 Partial | Shrink the package, each removal stating the promise it withdraws. −6,122 lines (−8.2%) executed; remaining: Tier A cleanup, housekeeping, and flipping the default profile to core. ([details](#codebase-consolidation)) |
| [CODE-REVIEW-2026-07-19.md](CODE-REVIEW-2026-07-19.md) | 🟡 Partial | Review of the terrain/vegetation/IBL/shadow-cache/tiles3d + forest-demo work since `9eb2ff1`; two passes landed the fixes. Still open: Draco decode-size cap, sampler-budget unification, duplicate tiles3d vegetation. ([details](#code-review-2026-07-19)) |
| [GPU-NURBS-TESSELLATION.md](GPU-NURBS-TESSELLATION.md) | 📋 Planned | GPU-based NURBS rendering using tessellation shaders |
| [RAYCAST-PICKING.md](RAYCAST-PICKING.md) | 🛑 Shelved | CPU ray-cast picking for large scenes: cached world BVH broad phase + geometry-delegated `intersect()` narrow phase (analytic primitives, triangle-BVH meshes, point/line tolerance); MRT/`GL_SELECT` buffer kept as fallback. Shelved — async MRT buffer path is enough (~0–10% overhead, O(1)/pick); real wall is the O(N) draw loop, addressed by instancing |
| Screenshots in the platform's pictures folder | ⬜ Todo | Save screenshots into the platform's pictures folder (XDG / Windows shell / `~/Pictures`) instead of the working directory, cwd kept as fallback; goes beside `appdatadirectory()` in `homedirectory.py`. ([details](#screenshots-in-the-platform-s-pictures-folder)) |
| Pointer capture on every backend | ⬜ Todo | Implement `setPointerCapture()` on GLUT, wxPython and Pygame (only GLFW has it today) so mouse-look works past the window edge on every backend. ([details](#pointer-capture-on-every-backend)) |
| Rebinding a node's `children` notifies nobody | ⬜ Todo | `node.children = [child]` silently fails to reach the renderer (the rebound list sends no signal). Fix at the field — an `MFNode` set that mutates the `OList` in place. Three in-tree call sites; the deeper-invalidate half already fixed. ([details](#rebinding-a-node-s-children-notifies-nobody)) |
| [USER-SETTINGS.md](USER-SETTINGS.md) | 🔶 Partial | Separate what the game shipped from what the player chose. Built: `open_settings` callback + `changed_fields()`. Left: a per-field `audience` hint, a `usersettings` load/save module, and a developer-only editor page. ([details](#user-settings)) |
| [ORDER-INDEPENDENT-TRANSPARENCY.md](ORDER-INDEPENDENT-TRANSPARENCY.md) | 📋 Planned | Implement order-independent transparency to eliminate sorting artifacts |
| [RUNTIME-IBL.md](RUNTIME-IBL.md) | 🟡 Partial | Per-object image-based lighting (each object lit by what it can see), baked at load and lazily re-baked on movement. RGBE `.hdr` + HDRI catalogue + `HDRBackground` shipped; the per-object probe capture and caching still planned. ([details](#runtime-ibl)) |
| [OMI-ENVIRONMENT-SKY.md](OMI-ENVIRONMENT-SKY.md) | 🟡 Partial | Drive the scene sky from the `OMI_environment_sky` glTF extension. Gradient / panorama / cubemap / plain backgrounds land; still open: the analytic physical sky, gradient sun tint, ambient contribution, and `KHR_animation_pointer`. ([details](#omi-environment-sky)) |
| [PBR-TEXTURE-LIBRARY.md](PBR-TEXTURE-LIBRARY.md) | 📋 Planned | Import freely-licensed CC0 PBR material sets (ambientCG / Poly Haven) into ready-to-use `PBRMaterial` nodes — ORM-packing + sRGB/normal handling, an import/cache/attach module, a bundled set with provenance, and a demo. ([details](#pbr-texture-library)) |
| [GLTF-FULL-FEATURE-CONFORMANCE.md](GLTF-FULL-FEATURE-CONFORMANCE.md) | 🟡 Partial | Render all 121 GLB Khronos glTF-Sample-Assets models correctly; the phased conformance push is done. Still deferred (research-grade): volumetric subsurface (ScatteringSkull) and nested-shell transmission (USDShaderBall). ([details](#gltf-full-feature-conformance)) |
| [TESTSUITE-IMPROVEMENTS.md](TESTSUITE-IMPROVEMENTS.md) | 🟡 Partial | Automated pytest suite with subprocess isolation, visual regression, and HTML reports. **2026-07-29:** a rendered baseline must not inherit the parent's rendering environment — `renderoptions.clean_environment` |
| Godot scene loading | ⬜ Todo | Load Godot scene descriptions (`.tscn`/`.escn`, and/or `.scn`) into the scenegraph — parse the node tree, transforms, meshes and materials into OpenGLContext nodes |
| Quake 3 BSP loading | 🛑 Shelved | A full Quake 3 BSP pipeline — lightmaps, PVS/cluster visibility, brush geometry — already runs in the `twig-bb/` project (an Open Arena-style game). The remaining engine work would be to lift that geometry loading into the core as a reusable `OpenGLContext` loader / `SceneAdapter`, so any app could open a `.bsp`/`.pk3` map the way `oglc-view` opens glTF/OBJ/VRML97/3D Tiles. **On hold:** the capability is proven in the demo and the value of a second, engine-side consumer looks minimal right now; revisit when another app actually needs to load BSP maps. |
| Retire render-visitor passes | ✅ Complete | The flat passes (`_flat.py`/`flatcore.py`/`flatcompat.py`) are the only rendering path. The visiting machinery — `RenderPass`/`VisitingRenderPass`/`PassSet` and `RenderVisitor`, plus their only consumers `browser/` and `shadow/` stencil — was removed in the 2026-08-01 consolidation (see [CODEBASE-CONSOLIDATION.md](CODEBASE-CONSOLIDATION.md)). Residual cleanup done 2026-08-16: `passes/rendervisitor.py` (viewpoint binding only) renamed to `passes/viewpointbinding.py`, and stale `RenderVisitor`/`renderpass.RenderPass`-class references scrubbed from `context.py`, `scenegraph/__init__.py`, `scenegraph/lod.py`, `events/*.py`, README and `docs/structure.html`. The optional `renderpass.py`→`dispatch.py` and `visitor.py`→`scenegraph/find.py` renames remain as CODEBASE-CONSOLIDATION B4 polish (a release-length re-export shim), not part of this entry. |
| [TILES3D-CITY-VIEWING.md](TILES3D-CITY-VIEWING.md) | 🟡 Partial | View a city-sized OGC 3D Tiles dataset (experimental). Up-axis / levelling / framing / speed fixes landed. Open: geometry floating above ground, a stray tile, no run-time speed control, over-fetching, ~30 fps at city scale. ([details](#tiles3d-city-viewing)) |
| [TERRAIN-SYSTEM.md](TERRAIN-SYSTEM.md) | 📋 Planned | Production-grade large-world terrain + vegetation, standards-first on OGC 3D Tiles (glTF mesh tiles in one octree, SSE LOD, async paging). Caves via Transvoxel, one triplanar splat material, instanced/impostor + GPU-grass vegetation. ([details](#terrain-system)) |
| Procedural backgrounds / environment | ⬜ Todo | Replace flat demo backgrounds with procedural environments — volumetric clouds, animated water, noise-heightfield terrain — so demos feel like real scenes rather than a gradient sky and empty floor. ([details](#procedural-backgrounds-environment)) |
| Void gravity suppression | 🟡 Partial | Stop a character hovering over a void: opt-in `suppressOverVoid` casts five down-rays against static AABBs and falls only when all miss. Built with tests; the interactive `physics_void.py` demo is still TODO. ([details](#void-gravity-suppression)) |
| [OMI-EXTENSIONS.md](OMI-EXTENSIONS.md) | 🟡 Partial | The roster of OMI 'world' glTF extensions and what each would take. Done: audio-emitter, all four physics, OGG-Vorbis. Opus read but not decoded. Planned: environment-sky; future: seat, spawn-point, vehicles. ([details](#omi-extensions)) |
| Navigation mesh | 🟡 Built, not yet usable on a level | A navmesh generated from the collision mesh at load time (never baked from content — the licensing and engineering answer): walkable cells, welded edges, A* + string-pulled paths. Builds on real levels, but `cell_at` spawn resolution fails on `oa_dm1`; no off-mesh links yet. ([details](#navigation-mesh)) |
| [VIEWER-COMPONENT-EXTRACTION.md](VIEWER-COMPONENT-EXTRACTION.md) | ✅ Complete | Give `bin/gltf_view.py`'s component a home: physics as a mixin on any interactive context, and a reusable `viewer/` glTF component. Script dropped 1,288→270 lines; the enabling phase of the one-viewer consolidation. ([details](#viewer-component-extraction)) |
| [ONE-VIEWER.md](ONE-VIEWER.md) | ✅ Complete | One viewer, `oglc-view`, for glTF / VRML97 / OBJ / 3D Tiles, format chosen by a registered `SceneAdapter`; the old per-format commands become aliases. Became a program — launch menu, model library, settings/controls — on the shared `ui` library. ([details](#one-viewer)) |
| [MARBLE-MADNESS-DEMO.md](MARBLE-MADNESS-DEMO.md) | ✅ Complete | `marble-demo`: an isometric Marble Madness re-interpretation (`oglc-marble`), built through 7 TDD phases. Procedural tracks, spin-steered PBR marbles under IBL, mechanisms, iso follow-cam; generic physics/camera additions landed in the engine. ([details](#marble-madness-demo)) |
| [CODE-REVIEW-2026-07-26.md](CODE-REVIEW-2026-07-26.md) | ✅ Complete | Review of the overlay-UI / movement-modes / settings-indirection / instancing changes since `8e7703a`. All 45 findings fixed Red/Green; suite green. Made the mypy gate runnable and fixed a read-before-set root cause behind three findings. ([details](#code-review-2026-07-26)) |
| [CODE-REVIEW-2026-01.md](CODE-REVIEW-2026-01.md) | ✅ Complete | Comprehensive code review with recommendations |
| [GOD-OBJECT-DECOMPOSITION.md](GOD-OBJECT-DECOMPOSITION.md) | ✅ Complete | Behaviour-preserving split of the 8 god objects flagged in CODE-REVIEW-2026-07-13 §3n/§7c (selection, indexedfaceset, nurbs, shaderpass, context, gltf, shadowmixin, _flat) into cohesive sibling modules/mixins; every import path unchanged, 340+ tests + off-screen renders green |
| [PYTHON-MODERNIZATION.md](PYTHON-MODERNIZATION.md) | ✅ Complete | Modernize to require Python 3.10+ and remove Python 2.7 compatibility |
| [SHADER-BASED-VRML97.md](SHADER-BASED-VRML97.md) | ✅ Complete | Shader-based rendering pass implementing VRML97 lighting model |
| [CORE-PROFILE-COMPATIBILITY.md](CORE-PROFILE-COMPATIBILITY.md) | ✅ Complete | Make `flatcore.py` truly core-profile compatible (delivered via SHADER-BASED-VRML97): shader render paths for all listed nodes, fixed-function calls gated behind `use_shaders`, tested via the compat/core conftest fixture. ([details](#core-profile-compatibility)) |
| [SELECTION-OPTIMIZATION.md](SELECTION-OPTIMIZATION.md) | ✅ Complete | MRT object-ID selection: colour+id+depth FBO, O(1) pick lookup wired into the forward pass, all shaders emitting object IDs. Async PBO readback (fence + map, ≤4 in-flight) implemented; only optional BVH hierarchical selection left. ([details](#selection-optimization)) |
| [INSTANCED-GEOMETRY.md](INSTANCED-GEOMETRY.md) | ✅ Complete | Collapse shapes sharing a geometry into one `glDrawElementsInstanced` (~2.1× faster, every instance still pickable) across 5 staged draw paths and primitives / PBRMesh / IndexedFaceSet, with cached per-frame buffers and cluster culling. ([details](#instanced-geometry)) |
| Click-through + unlit picking | ✅ Complete | `Shape.pickable=False` masks the object-id MRT attachment so picks read through (water/gizmo/annotation). Also made unlit geometry pickable (PBR `KHR_materials_unlit` + `vrml97_unlit`, object-id follows program switches) and fixed a finding-4.6 blend bug zeroing point ids. 12 new tests |
| [MOVEMENT-MODES.md](MOVEMENT-MODES.md) | ✅ Complete | Walking / flying / swimming / mouse-look as declared PROTO nodes with typed fields, driven by a once-per-frame input sampler; `enter_when()`, modifier key bindings, pointer capture, and a rebindable table. Wired into the viewers. ([details](#movement-modes)) |
| [OVERLAY-UI.md](OVERLAY-UI.md) | ✅ Complete | In-window overlay UI (settings, key bindings, console, licence) with a full widget/box/scroll set and nine-slice skins. Widgets edit a copy of a node via `SettingsSession`; the settings page is generated from `ContextDefinition` fields; the interface scales with the window. ([details](#overlay-ui)) |
| [HUD-DEBUG-OVERLAY.md](HUD-DEBUG-OVERLAY.md) | ✅ Complete | Screen furniture over a live world plus a provider-fed developer overlay replacing the frame-rate display. `HUDLayer` is anchored, non-modal, drawn under panels; crosshair / bar / readout / message widgets; subsystems register providers. 93 no-GL + 7 driver tests. ([details](#hud-debug-overlay)) |
| [LOOP-INSTRUMENTATION.md](LOOP-INSTRUMENTATION.md) | ✅ Complete | Measure what the frame rate hides: `looptrace.LoopTrace` charges wall-clock per named phase (summing to the iteration) and keeps the worst/stall data the fps median throws away, read against fps. Plus threaded-sim rate and twig-bb's frame-clock accounting. ([details](#loop-instrumentation)) |
| [SHADOW-MAPPING.md](SHADOW-MAPPING.md) | ✅ Complete | High-quality shadow mapping in the VRML97 shader: spot (2D), directional (CSM), point (omni cube), PCF + PCSS soft shadows, range culling. 58 tests. VSM/EVSM deferred (documented reason) |
| [PERFORMANCE-SHADOW-REGRESSION-2026-07.md](PERFORMANCE-SHADOW-REGRESSION-2026-07.md) | ✅ Complete | Parthenon 60→20 fps traced to `renderShadowMaps` re-culling/re-drawing all 287 casters uncached (68% of frame). R1–R5 (caching, reuse, cull-once, hoist/vectorize) cut the shadow pass −72% walk / −43% spin, pixel-identical. ([details](#performance-shadow-regression-2026-07)) |
| [PBR-BRDF-CONFORMANCE.md](PBR-BRDF-CONFORMANCE.md) | ✅ Complete | Term-by-term audit of the PBR uber-shader and all 19 glTF extensions against Appendix B, the specs and the Khronos reference, then implemented. Core BRDF conformant; a long fix list (spotlight cone, sheen, specular f90, iridescence, clearcoat, volume, transmission…) landed. Only `KHR_texture_basisu` excluded. ([details](#pbr-brdf-conformance)) |
| [PBR-MATERIALS.md](PBR-MATERIALS.md) | ✅ Complete | PBR metallic/roughness pass + `PBRMaterial` + glTF/GLB loader (URL streaming) + Khronos sample-model viewer; textures/normal/emissive + shadows. 16 tests. Proper metal rendering shipped (dual-mechanism IBL: `full` probe + corrected `analytic`, `OPENGLCONTEXT_IBL` switch + fps-adaptive degradation — §9). Per-object IBL is the open follow-on, tracked in [RUNTIME-IBL.md](RUNTIME-IBL.md) |
| [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md) | ✅ Complete | Close the gap to Khronos reference renders: animation, skinning, morph targets, real IBL, mipmaps, alpha/transmission, second UV, sparse accessors, computed tangents, punctual lights, and the full `KHR_materials_*` family, with async load + auto-framing. ([details](#gltf-complete-support)) |
| [DRACO-COMPRESSION.md](DRACO-COMPRESSION.md) | ✅ Complete | Decode `KHR_draco_mesh_compression` via the optional `DracoPy` library (decode when present, warn-and-skip otherwise), verified against the Khronos Duck Draco variant with unit / missing-library / render tests. ([details](#draco-compression)) |
| [GLTF-REMOTE-RESOURCES.md](GLTF-REMOTE-RESOURCES.md) | ✅ Complete | The `oglc-gltf` viewer's URL path now goes through `load_gltf_url()` (resolver-backed), so remote multi-file `.gltf` documents resolve their external `.bin`/textures against the document origin; remote `.glb` unchanged; off-origin refs rejected. `_download` (single-file) removed; verified against Khronos `BoxTextured` + `TestRemoteSourceRouting` tests |
| [GLTF-DEMO-CONFORMANCE.md](GLTF-DEMO-CONFORMANCE.md) | ✅ Complete | Acceptance track for 100% Khronos-sample reference parity: `test_gltf_conformance.py` gates every sample (149 scenes / 158 views) against blessed baselines. Two waived pending features (subsurface, nested-shell transmission). ([details](#gltf-demo-conformance)) |
| Teapot UV coordinates | ✅ Complete | Per-vertex UV + PBR on the Teapot: surfaces the GLU tessellator's (u,v) via a texcoord callback, the Utah injective-with-interior layout, and tangents — the first PBR material on non-glTF geometry. Works in core and compat. ([details](#teapot-uv-coordinates)) |
| [TERRAIN-ASSET-FORMATS.md](TERRAIN-ASSET-FORMATS.md) | ✅ Complete | Reference: the input asset formats for `SplatTerrain`/`HeightField` and the forest demo's terrain provenance — the 16-bit Terrarium heightmap, the RGBA splat control map, the CC0 detail materials, with runnable fetch/build recipes. ([details](#terrain-asset-formats)) |
| Animated PBR surfaces | ✅ Complete | A material-driven surface deformer on `PBRMesh` (distinct from morph/skinning): rides the existing dynamic-VBO pipeline, always sees the rest pose and gets copies, and re-uploads UVs only when they move. Enables Quake 3 `.shader` animation. 22 no-GL tests. ([details](#animated-pbr-surfaces)) |
| [SPATIAL-AUDIO.md](SPATIAL-AUDIO.md) | ✅ Complete | Sound placed in the scene on glTF's `KHR_audio_emitter` model, shipped as the standalone numpy-only `omi_audio` package with OpenGLContext integrations (VRML `Sound`/`AudioClip` finally audible). Own numpy mixer — no copyleft dependency; silence is a backend. 336 tests. ([details](#spatial-audio)) |
| [PARTICLE-EFFECTS.md](PARTICLE-EFFECTS.md) | ✅ Complete | Fire / smoke / sparks / explosions / trails as camera-facing quads — one instanced draw per system. `ParticlePool` packs the living at the front for a single upload; per-system colour/size curves; eye-space billboarding; a shader dot needs no asset. 71 no-GL tests. ([details](#particle-effects)) |
| Fog, and swimming | ✅ Complete | Two long-declared nodes finally driven: VRML97 `Fog` rendered through the PBR shader (both curves, binds like a viewpoint), and swimming as a real `SwimMode` + character swim step rather than a use of noclip. 45 tests across nodes, driver, modes and swim. ([details](#fog-and-swimming)) |
| [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md) | ✅ Complete (all 8 phases, 0–7) | Real-time rigid-body physics built natively on the OMI glTF physics model — the `physics/` package (broad/narrow phase, GJK/EPA, sequential-impulse solver, islands, sleeping, joints, triggers, character controller), mesh cooking, and a GL-compute backend. 91 tests, 9 demos, all 8 phases. ([details](#physics-collision)) |

## Details

Full notes for the entries above, in table order. Where a plan document exists it is linked in the heading and holds the authoritative design.

<a name="handoff-forest-performance-2026-08-12"></a>
### [HANDOFF-FOREST-PERFORMANCE-2026-08-12.md](HANDOFF-FOREST-PERFORMANCE-2026-08-12.md) — 🟡 Partial

Hold 60 fps in the `oglc-forest` demo on an integrated GPU (Intel UHD 630). **Tier 1 done & committed** (forest `main` `bfa5556`, engine `develop` `01b2201`): render-quality presets `high/medium/low` + a measured-frame-rate `auto` picker (down-only; discrete GPU holds high, UHD 630 lands on medium, weaker on low; shadows kept — they cost <1 ms) in the demo's `quality.py`, and **off-thread stream recompute** (`streaming.py::AsyncStreamer` + `scene.py` compute_*/apply_* split + engine `InstancedMeshLOD.compute_pending`) that moved the ~50–100 ms camera-following scatter off the render thread (worst-case render-thread scatter 103 ms → 0.14 ms, 0 frames > 80 ms). Diagnosis: grass clumps ≈70% of GPU (~13 M tris/frame); at native 1280×800 the UHD 630 has a ~14 ms fill-rate floor, so earlier small-window numbers were ~2× optimistic. **Tier 2 (engine, not started, benefits twig-bb too):** finish the glGet-state removal on the veg/terrain/particle nodes (`instancedgl.save_draw_state`, `vegetation/base.py` — the pass path was migrated, these were not), `glMultiDrawElementsIndirect` for the 26 near-mesh + 13 impostor draws, `ARB_buffer_storage` persistent triple-buffered `InstanceBuffer`, per-species program cache, BC7 textures. twig-bb still unprofiled

<a name="codebase-consolidation"></a>
### [CODEBASE-CONSOLIDATION.md](CODEBASE-CONSOLIDATION.md) — 🟡 Partial

Shrinking the package, each removal stating what promise is withdrawn. **Executed 2026-08-01: 75,045 → 68,923 lines (−6,122, −8.2%)** — `browser/` (VPython API + `oglc-visual`), `shadow/` (stencil volumes), `scenegraph/tree/` (volumetric tree), the **visiting render passes** (`renderpass.py` 937→80, `rendervisitor.py` 515→61, `visitor.py` 389→68), `DisplayListCompiler`, `nurbsshader`, `move/fps`, `passes/flat` — plus 386 lines of demos and the 543-line `docs/renderprocess.html`. `browser.homedirectory.appdatadirectory` extracted to `OpenGLContext/userpaths.py` (Red/Green) with the sibling `twig-bb` project's import updated. Full suite **3,825 passed / 0 failed**, ruff clean on every touched file, both profiles verified rendering live. Also fixed a latent defect found en route: `DisplayList.__del__` raised `GL_INVALID_OPERATION` when its context had already gone. **Still open:** B6 (`oglc-view` consolidation), C1 (housekeeping), rest of Tier A. **B5b declined by design.** **Bounded first by a support commitment now written into `readme.txt` and [docs/structure.html](../docs/structure.html): GLFW, GLUT, Pygame, wxPython and Qt/PySide (via `OpenGLContext_qt`) are first-class, and platform-specific code is supported on the platform it targets whether or not this container can run it** — which withdrew an earlier draft's proposals to drop WGL fonts, `glutfont`, `wxfont` and the wx backend, and turned "no CI for three of five backends" into a testing gap to close rather than a support level to lower. Measured with an AST import graph over all 858 modules, cross-checked against the string-keyed `Node()`/`Loader()` plugin registries the graph cannot see. **Tier A** — genuinely dead: `nurbsshader.py` (superseded by the wired-in `nurbstess.py`), `events/tkevents`+`fxevents` (no Tk/FOX context exists to reach them), `move/fps`, `passes/flat`, ten dead `resources/*` blobs. **Tier B** — the named candidates: `browser/` (held in place solely by a 76-line `appdatadirectory`; withdraws the unfinished VPython API + `oglc-visual`), `shadow/` (stencil volumes — one non-entry-point consumer, zero tests, zero docs), `scenegraph/tree/` (TERRAIN-SYSTEM already recorded VolumetricTree as rejected), the **visiting render passes** (`_flat.FlatPass` does not inherit from `RenderPass` at all — 1,710 of 1,839 lines die once `browser/` and `shadow/`, their only consumers, go; retires `docs/renderprocess.html`), and **display lists**, which split in two: **B5a** `DisplayListCompiler` is unreachable — `ArrayGeometryCompiler` inherits weight 1.0 and the display-list one returns 0.9, so it never wins the max — and goes for free; **B5b**, removing the legacy scenegraph renderer, is **recommended against**, because `flatcompat` + `displaylist` + `text/font.py` are how three of the five supported backends reach their native font provider (`glutfont`/`wxfont`/`wglfont` are the compatibility-profile Text path, already gated by `fontprovider`'s `shader_compatible` flag). The 66 raw-GL tutorials are unaffected either way — they need a compatibility *context*, not the pass. What replaces B5b is **flipping the default profile to `core`** (74 scenegraph demos currently take the legacy path by default) and re-blessing — the highest-value step here, and it deletes nothing. Plus **viewer consolidation** into one content-type-dispatched `oglc-view` reusing the existing `plugins.Loader` MIME registry (all four in-tree backends already request core when asked, so none blocks it). **Tier C:** `bin/` non-tools, the second in-wheel test root, packaging leftovers. Includes sequencing, per-item doc impact, a **Withdrawn candidates** table, and a "not a candidate" list recording why plugin-registered nodes and the dynamically-imported font atlases only *look* orphaned

<a name="code-review-2026-07-19"></a>
### [CODE-REVIEW-2026-07-19.md](CODE-REVIEW-2026-07-19.md) — 🟡 Partial

Critical review of all changes since `9eb2ff1` (terrain/vegetation/IBL/shadow-caching/tiles3d + forest-demo). 15 ranked + ~40 per-subsystem findings, each with a status. **Pass 1 (2026-07-19):** ranked 1–4, 6–7, 9, 11–15 (IBL `_build` exception-safety, both shadow cache bugs, `dispose()` + retained handles, `ensure_gl` fallback, GL-baseline doc, `castsShadow` depth filter, heightfield bit-depth, in-memory clump texture, integer scatter hash, local GL save/restore, terrainwalk broadphase). **Pass 2 (2026-07-20):** V1/V4–V7, T1/T3–T7, I1/I4–I8, S2/S4, G1–G6, X1/X2/X4/X5, D1–D3 — vegetation `InstancedVegBase` dedup + shared `LOD_NEAR/FAR` uniform + `InstanceBuffer`, world-up ambient, IBL float-render capability probe + LDR skybox fallback + shared `dirToEquirect` include + split `_build`, Draco hardening, `cc0`→`loaders/`, and the **forest-demo** made importable/reusable (`ForestConfig`+`build_forest_scene`→`ForestScene`, argparse CLI, bundled clump asset) for a future driving demo. All with tests; 388 affected + 403/4-xfail render + 8 demo tests green. **Still open: ranked 5** (Draco decode-size cap), **8** (sampler-budget unification), **10** (prune duplicate tiles3d vegetation); lower-sev V2/T2/I3/X3, S1 partial

<a name="screenshots-in-the-platform-s-pictures-folder"></a>
### Screenshots in the platform's pictures folder — ⬜ Todo

`Context.OnSaveImage` (Alt+S) writes into the **working directory**, named for the program that took it (`oglc-gltf-screen-0001.png`) — which is right for a command run from a terminal and wrong for a game someone launched from a menu, where the working directory is arbitrary and may not be writable. Wanted: the platform's own pictures folder — `XDG_PICTURES_DIR` from `~/.config/user-dirs.dirs` with `~/Pictures` as the fallback on Linux, `SHGetKnownFolderPath(FOLDERID_Pictures)` on Windows, `~/Pictures` on macOS — with the working directory kept as the fallback when none resolves or the folder cannot be written. **`OpenGLContext/browser/homedirectory.py` is where this goes**: it already does exactly this shape of lookup for the application-data directory, including the Windows shell-folder call, so this is one more function beside `appdatadirectory()` rather than new machinery. Two things to settle while doing it: whether a game names a subfolder of its own (`~/Pictures/twig-bb/`), and whether `--capture` and the per-viewer `F2` handlers (which have their own iso-dated naming) should follow the same rule — today they are deliberately cwd-relative because a script redirecting them wants that. The naming and the directory are separate decisions and this is only the directory one

<a name="pointer-capture-on-every-backend"></a>
### Pointer capture on every backend — ⬜ Todo

`setPointerCapture()` is implemented only for GLFW (`glfw.CURSOR_DISABLED` + `RAW_MOUSE_MOTION` where supported); `ViewPlatformMixin.setPointerCapture` returns False everywhere else, so a mouse-look `FPSMode` on GLUT, wxPython or Pygame turns only as far as the window edge and the cursor stays visible over the view. Wanted: `glutcontext` (`glutSetCursor(GLUT_CURSOR_NONE)` plus warp-to-centre, since GLUT has no unbounded-motion mode — the warp must not read as user motion), `wxcontext` (`CaptureMouse`/`ReleaseMouse` + `wxCURSOR_BLANK`, and GTK3 needs the capture released before any modal or the app deadlocks), `pygamecontext` (`pygame.event.set_grab` + `mouse.set_visible(False)`, which gives real relative motion via `MOUSEMOTION.rel`). Each returns True only if it genuinely grabbed, since `_applyPointerCapture` treats False as "this backend cannot" and stops asking. Tests exist for the GLFW path (`tests/unit/test_pointer_capture.py`) and are the pattern to copy. See [MOVEMENT-MODES.md](MOVEMENT-MODES.md)

<a name="rebinding-a-node-s-children-notifies-nobody"></a>
### Rebinding a node's `children` notifies nobody — ⬜ Todo

`node.children = [child]` is the natural way to write a scenegraph change and it **silently fails to reach the renderer**. `FlatPass` walks the scenegraph once (`SGObserver`) and thereafter keeps its set of renderable paths current from the `OList.NEW_CHILD_EVT` / `DEL_CHILD_EVT` signals a node's `children` list sends as it is added to and removed from. Assigning a *new* list to the field replaces the observed one instead of mutating it, so no signal is sent and the pass goes on drawing whatever it last integrated: the scenegraph is correct, the screen is stale, and nothing anywhere reports a problem. `children[:] = [child]` is the spelling that works, which is a distinction no caller should have to know. **Confirmed in the wild** — it is what made twig-bb draw every weapon with the model of whichever one was held when the level loaded (that project's B32), invisible until somebody switched weapon at *runtime*, since a scene integrated after the change looks right. **Three call sites in this package have the same shape**: `physics/debugdraw.py` rebinds `_proxy_group.children` when new proxies appear, `scenegraph/tilesterrain.py` rebinds `self.children` as tiles stream in, and `bin/gltf_demo.py` rebinds a layer's — each drawing a stale set until something else happens to re-integrate. Wanted: fix it **where the field is set**, so the obvious spelling is the correct one — an `MFNode` set that mutates the existing `OList` in place (and so emits add/remove per element) rather than binding a new list, in pyvrml97's field machinery. That is shared 28-year-old code, so it wants its own Red/Green pass over what currently depends on identity-of-list rather than contents, plus a decision on whether the same rule should hold for every `MF*` field or only for the node-valued ones. The **other half of this defect is already fixed** (2026-08-03): `NodePath.invalidate` was marking only children and grandchildren broken because `iterdescendents` walked two levels against its own docstring, so a detached subtree deeper than that stayed in the draw set; `iterdescendents` now walks every generation and pyvrml97's own test, which had pinned the two-level behaviour, is corrected. Verified against this package's 4,703 unit tests

<a name="user-settings"></a>
### [USER-SETTINGS.md](USER-SETTINGS.md) — 🔶 Partial

Separating **what the game shipped** from **what the player chose**, which today are one `ContextDefinition` and indistinguishable once written — so a saved profile either freezes every default the game may want to change in a later build, or loses the player's choices at exit. **Built:** `settings.open_settings(context, on_apply=...)` calls back with the session once the draft is committed, and `SettingsSession.changed_fields()` reports only what the user moved (measured against the values the screen opened with, so it is still right after the commit) — enough for a game to persist settings correctly today. **Left:** an `audience` UI hint per field (`player`/`developer`), a `usersettings` module following `move.bindingstore` (load over the shipped definition at start-up, save only the changed player fields, forgiving on load), and a developer-only editor page. Precedence stays environment → shipped → saved

<a name="runtime-ibl"></a>
### [RUNTIME-IBL.md](RUNTIME-IBL.md) — 🟡 Partial

**Per-object image-based lighting**: give *each object* its own irradiance + pre-filtered reflection maps computed from its own location (à la [diharaw/runtime-ibl](https://github.com/diharaw/runtime-ibl)) so it's lit by *what it can see* — the walls/floor and the scene's `Background` cube/sphere — not one global environment. **Cost model:** bake at **load time** for static geometry; **lazily re-bake** only the few objects that move a non-trivial distance, cached against a **down-sampled AABB centre** and regenerated only when the object drifts past that centre (not every frame, rate-limited). Capture per-object cubemap → irradiance convolution → GGX pre-filter mip chain + shared BRDF LUT → feed the existing `OPENGLCONTEXT_IBL=full` uniforms. **Radiance RGBE `.hdr` support shipped:** numpy-only RGBE decoder (`loaders/hdr.py`), CC0 Poly Haven HDRI catalogue (`loaders/hdri.py`), equirect→env-cube projection feeding the existing IBL probe (`ibl_equirect.frag`), an `HDRBackground` node (URL-loaded via the Resolver, ACES-tone-mapped equirect skybox, registers itself as the IBL radiance map), and `oglc-gltf --environment <name|URL|.hdr>` wiring — verified end-to-end on metal glTF spheres against Poly Haven studio/outdoor panoramas. Still planned: the per-object probe capture + load-time/lazy caching. Ties into [PBR-MATERIALS.md](PBR-MATERIALS.md) §9 IBL, [PBR-TEXTURE-LIBRARY.md](PBR-TEXTURE-LIBRARY.md) HDRIs, [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md) mover tracking, and GLTF EnvironmentTest parity

<a name="omi-environment-sky"></a>
### [OMI-ENVIRONMENT-SKY.md](OMI-ENVIRONMENT-SKY.md) — 🟡 Partial

Load the [`OMI_environment_sky`](https://github.com/omigroup/gltf-extensions/tree/main/extensions/2.0/OMI_environment_sky) glTF extension and drive the scene's sky from it. **Landed:** `loaders/gltf/environment_sky.py` reads the document's `skies[]` and the active scene's `sky` index into typed records and builds the background each describes — **gradient**→`Background` (the VRML97 gradient sphere), **panorama**→`HDRBackground` (equirectangular, which also registers the panorama with the IBL probe) or `CubeBackground` (cubemap, in the spec's `+X,-X,+Y,-Y,+Z,-Z` order), **plain**→`SimpleBackground` — hung off the scene root where the ordinary Background pass binds it, with `GLTFScene.sky` carrying the record either way. Two details settled by test: gradient stops are spaced evenly in *colour* rather than in angle (a `topCurve` of 0.15 turns almost entirely within a few degrees of the horizon, and evenly-spread stops draw that as a kink), and the panorama is rolled a quarter of its width at load because the extension puts the texture's middle at `+Z` where `dirToEquirect` puts `+X` — converted once, so the drawn sky and the reflections it drives keep one shared mapping. Found and fixed en route: `viewer.environment.count_backgrounds` counted only the VRML97 `Background` class, so a scene whose sky was any *other* backdrop got a second one added on top. 56 loader tests (module at 100%) + 3 rendered-frame tests. **⬜ TODO — the physical sky:** an analytic Rayleigh/Mie single-scattering skydome driven by the scene's directional sun, in glTF inverse-metre units, baked to a low-res equirect for the IBL probe. It is the one sky type that is a *renderer* rather than a translation; its parameters are parsed and reported so the record is there when the shader arrives. **Also still open:** the gradient **sun tint** (`sunAngleMax`/`sunCurve`), which is azimuthal and so cannot go on the elevation-only colour stops; the **ambient contribution** (`ambientLightColor`/`ambientSkyContribution`), read and kept but not wired to the ambient term; and `KHR_animation_pointer` on the colour/curve fields. Docs: [docs/gltf.html](../docs/gltf.html#sky). Ties into [RUNTIME-IBL.md](RUNTIME-IBL.md), [OMI-EXTENSIONS.md](OMI-EXTENSIONS.md)

<a name="pbr-texture-library"></a>
### [PBR-TEXTURE-LIBRARY.md](PBR-TEXTURE-LIBRARY.md) — 📋 Planned

Import freely-licensed **CC0 PBR material sets** (ambientCG `.zip`s, Poly Haven, etc.) into ready-to-use `PBRMaterial` nodes so demos/samples stop hand-baking textures. Core work is format translation: CC0 ships separate Color/Roughness/Metalness/AO/Normal maps, glTF wants them **ORM-packed** (G=roughness, B=metallic, R=occlusion) with correct sRGB/linear + GL(+Y)-normal handling. Adds an import+cache+attach module (`loaders/material_library.py`), a bundled/fetchable CC0 material set with a licence-provenance manifest, and a spheres-in-a-row demo. Also surveys authored-*material* sources (glTF `KHR_materials_*`, **MaterialX**, HDRIs for IBL) as future standards-aligned imports. Ties into [PBR-MATERIALS.md](PBR-MATERIALS.md) + [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md)

<a name="gltf-full-feature-conformance"></a>
### [GLTF-FULL-FEATURE-CONFORMANCE.md](GLTF-FULL-FEATURE-CONFORMANCE.md) — 🟡 Partial

Render **all 121** GLB-shipping Khronos glTF-Sample-Assets models correctly (roster expanded from 23; migrated deprecated `glTF-Sample-Models`→`glTF-Sample-Assets` repo; `EXT_texture_webp` source fallback landed). Phased conformance push done: framing (VirtualCity/PotOfCoals/RecursiveSkeletons), **P1 punctual light photometric model** (inverse-square + `range` + candela/lux scale), P2 `KHR_materials_volume` thick-wall refraction (Glass\*/USDShaderBall), P3 background-at-infinity, P4 deterministic animation capture + `KHR_animation_pointer` + `KHR_node_visibility`, P5 `diffuse_transmission` + `dispersion`, P6 sheen/specular tint. Plus 2nd-pass `KHR_materials_anisotropy`, studio env, spotlight-cone fix + emissive bloom. **Still deferred (research-grade):** full volumetric subsurface (ScatteringSkull SSS) and nested-shell transmission (USDShaderBall)

<a name="tiles3d-city-viewing"></a>
### [TILES3D-CITY-VIEWING.md](TILES3D-CITY-VIEWING.md) — 🟡 Partial

**Viewing a city-sized OGC 3D Tiles dataset, marked experimental in the docs.** Landed 2026-08-04, each red-first: `asset.gltfUpAxis` honoured, so a conforming dataset no longer renders on its side (measured — the building height axis was 90° from local up); geospatial datasets levelled as well as recentred; `framing.look_from` aim corrected (it pitched as far above the target as it should have been below, and the Sponza conformance baseline had been blessed with the fault); movement speeds sized to the framed scene *including* the free-fly manager's fixed 0.25-unit step, which is what the settings screen could never reach; a shift-held fly boost; a dataset opens over its content rather than 19 km outside its bounding sphere; a metric dataset gets a 1.8 m avatar instead of a 320 m giant; and `g` drops in from the camera rather than teleporting to the middle of the world. Verified against `toronto-3dtiles/` — 83,064 buildings in 436 tiles plus 241 ground-only tiles, map-textured, 50 MB, ODbL — which passes the reference `3d-tiles-validator` clean and streams 152 loads / 92 evictions under an 8 MB budget. **Open:** geometry floating above its ground in places; one tile placed far from the rest; no run-time speed control, so walking a city is impractical; the initial load fetching far more than the view needs (every tile shares `geometricError` 512); and ~30 fps at city scale (>1,000 draws, one 512×512 texture per ground tile)

<a name="terrain-system"></a>
### [TERRAIN-SYSTEM.md](TERRAIN-SYSTEM.md) — 📋 Planned

Production-grade large-world terrain + vegetation. **Standards-first, 3D-Tiles-native**: an OGC **3D Tiles** streaming runtime (`tileset.json` quadtree/octree BVH + screen-space-error LOD + async paging + LRU eviction) where **surface, caves, overhangs and props are all glTF mesh tiles** in one octree (rendered by the existing conformant glTF loader). Runtime ElevationGrid/GPU-tessellation dropped (can't displace a baked mesh); heightfields/DEMs are an *offline* bake input only. Costs paid explicitly: discrete-LOD popping → **geomorphing**, mesh weight → **Draco/meshopt** compression ([DRACO-COMPRESSION.md](DRACO-COMPRESSION.md)). **Caves** = glTF octree tiles, authored or **Transvoxel**-baked from `heightfield_sdf − Σ cave_brushes` (compute on 3060, CPU+cache elsewhere) → fissures/crevasses/overhangs. One **triplanar PBR** splat material reusing CSM/IBL; per-tile physics `trimesh` colliders (walkable caves). **Vegetation**: instanced glTF via `EXT_mesh_gpu_instancing` + octahedral impostors (trees/brush), compute-scatter + `glDrawElementsIndirect` GPU grass/flowers (VolumetricTree rejected as too slow). Capability tiers: RTX 3060 primary, modern iGPU, GL 3.3/software floor (surface path identical across tiers)

<a name="procedural-backgrounds-environment"></a>
### Procedural backgrounds / environment — ⬜ Todo

Replace the flat, artificial-looking demo backgrounds with lively procedural environments: fractal/volumetric clouds (FBM noise skydome or raymarched), choppy animated water (Gerstner waves / FFT ocean with reflections), and procedurally generated ground/terrain (noise heightfield with texture-splatting). Goal is demos that feel like a real scene instead of a plain gradient sky + empty floor. Ties into [ROAM / modern terrain] for the ground and [ORDER-INDEPENDENT-TRANSPARENCY.md](ORDER-INDEPENDENT-TRANSPARENCY.md)/water reflections

<a name="void-gravity-suppression"></a>
### Void gravity suppression — 🟡 Partial

Character hovers instead of dropping when no geometry lies below it. Opt-in `CharacterCapabilities.suppressOverVoid` (default off); `CharacterController._over_void()` casts five infinite straight-down rays (capsule base centre + four radius-ring points) against static-collider **AABBs** — a void is where *every* ray misses, so a footprint still partly on ground falls/slides off normally. The gravity step is gated on `vy <= 0` so a jump *across* a void still arcs up. AABB-level only (won't see holes inside one collider's bounding box; errs toward "fall" when unsure). No new raycast/BVH — reuses the controller's cached static proxies. `tests/test_character_void.py` (7 tests). Interactive `physics_void.py` demo still TODO. See [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md)

<a name="omi-extensions"></a>
### [OMI-EXTENSIONS.md](OMI-EXTENSIONS.md) — 🟡 Partial

The roster: which of the [OMI group's glTF extensions](https://github.com/omigroup/gltf-extensions/tree/main/extensions/2.0) — the ones describing a *world* rather than a model — are read, and what the rest would take. **Done:** `KHR_audio_emitter` and all four `OMI_physics_*` (shape/body/gravity/joint, read *and* written), plus **`OMI_audio_ogg_vorbis`**, which lands here: a source may name a second `audio` entry holding the same sound better encoded, and `omi_audio.formats` + `AudioLibrary.clip_for()` now ask for the best encoding this build decodes, fall back to the MP3 when one will not *resolve*, and wait rather than fall through on one that is merely still downloading. `formats.decodable()` asks the backend what it reads instead of asserting a list, so a `miniaudio` that gained Opus would be used with no code change. **`OMI_audio_opus` is read, round-tripped and reported but not decoded** — it needs a second decoder plus WebM demuxing, and its sources play their MP3 fallback. **Planned:** `OMI_environment_sky` ([OMI-ENVIRONMENT-SKY.md](OMI-ENVIRONMENT-SKY.md)), whose gap is a loader rather than a renderer — three of its four sky types are backgrounds already drawn. **Future work:** `OMI_seat`, `OMI_spawn_point` and the four `OMI_vehicle_*`, each sketched with what it would need (spawn points map onto `Viewpoint`; a seat needs a rider/occupancy seam the movement modes lack; vehicles want that seam plus a wheel model). Docs: [docs/gltf.html](../docs/gltf.html#omi), [docs/audio.html](../docs/audio.html#codecs)

<a name="navigation-mesh"></a>
### Navigation mesh — 🟡 Built, not yet usable on a level

`OpenGLContext.nav.navmesh`: a navmesh **generated from the collision mesh at load time**, never baked and never read from content — which is the licensing answer and also the better engineering one, since it gives navigation for levels nobody ever baked, regenerates when geometry changes, and depends on nothing we may not read. Walkable triangles by slope and facing; neighbours by shared edge, **welded by position** because a collision mesh from a level is a triangle soup where the same corner has a different index in every triangle that touches it — matching indices joins nothing and leaves every cell an island; A* over the cells and a **string-pulled** path through the portals, because a path along cell centres zigzags across an empty room and every corner it rounds is one it did not need. 25 tests, no GL: an open floor gives a straight line, an obstacle is routed round, a ramp is climbed, an island is unreachable. **On `oa_dm1` it builds 1220 connected cells in 0.03 s and then fails at the last step — no spawn point resolves to a cell** (`cell_at`, and the coordinates line up, so it is the point-in-triangle test or the slope filter). The headroom test is too blunt for a real level and is off by default: it compares bounding boxes where it wants a distance, and removed 1092 of 1220 cells almost none of which were near a wall. No off-mesh links yet, so jump pads are not edges. Next steps are in twig-bb's PROJECT-PLAN §3b T2

<a name="viewer-component-extraction"></a>
### [VIEWER-COMPONENT-EXTRACTION.md](VIEWER-COMPONENT-EXTRACTION.md) — ✅ Complete

Give the 1,288-line `bin/gltf_view.py` script's *component* a home. Two layers: **physics as a capability of any interactive context** (`move/physicswalk.py::PhysicsWalkMixin` — `enablePhysics`, `stepPhysics`, the `buildPhysicsWorld` seam, `spawnAvatar` clear-floor placement, `moveAvatarToViewpoint`), mixed into `ViewPlatformMixin` so every context has it and none pays for it until asked; and a **reusable glTF viewing component** (`OpenGLContext/viewer/` — `ViewerOptions` as the one options type for both the library and the CLI, format-neutral `AsyncSceneMixin`, overlay/screenshot/settle-capture mix-ins, pure framing maths, and `GLTFViewerContext`). `bin/gltf_view.py` drops to ~300 lines of argument definitions and `main()`. No console script renamed, added or removed. **Landed 2026-08-02**: `bin/gltf_view.py` 1,288 → 270 lines, full unit suite green (4,153), ruff + mypy clean, new modules at 100% coverage bar the live-GL frame loop. Found and fixed a real `omi_physics` defect en route (`safe_bind` reported a stale `grounded`, so binding to an aerial viewpoint started the avatar falling instead of flying). Enabling phase of [CODEBASE-CONSOLIDATION.md](CODEBASE-CONSOLIDATION.md) §B6, which is now adapters + CLI only

<a name="one-viewer"></a>
### [ONE-VIEWER.md](ONE-VIEWER.md) — ✅ Complete

Finish [CODEBASE-CONSOLIDATION.md](CODEBASE-CONSOLIDATION.md) §B6: **one viewer, `oglc-view`**, for glTF, VRML97, OBJ and 3D Tiles. Format is chosen from the source by a `SceneAdapter` registered under a new `plugins.Adapter`, so dispatch is data and a third party adds a format without touching the viewer. `GLTFViewerContext` → `ViewerContext`; `oglc-gltf`, `oglc-vrml` and `oglc-tiles` become deprecating aliases. The viewer became a *program*: a launch menu, a 191-entry library of models and worlds shown by their own pictures (derived from the existing demo roster, not authored again), a settings and a controls page — all on the shared `ui` library, with the caption and the demo browser's reference thumbnail moved off ~200 lines of hand-rolled drawing onto HUD widgets. New `ui.pictures.PictureCache` fetches, decodes off the render thread and LRU-evicts, so a gallery of hundreds is bounded. **Landed 2026-08-02**, verified live on GLFW, Pygame and Qt in core profile and on the compatibility pass. Five real defects found and fixed red-first — a redraw recursing into `OnDraw` during `OnInit`, an OBJ loader that could not read any file, `--capture` hanging under a compositor, the compatibility pass drawing no screen at all, and `resolve_source` exiting the process at runtime

<a name="marble-madness-demo"></a>
### [MARBLE-MADNESS-DEMO.md](MARBLE-MADNESS-DEMO.md) — ✅ Complete

`marble-demo` scene package: an isometric Marble Madness re-interpretation, **built through all 7 phases with Red/Green TDD (77 tests green), playable via `oglc-marble`**. Procedurally generated tilted tracks (surface patches, ramps, rails), pure-spin arrow-key steering with a pairwise marble↔surface friction grid, real ramp jumps, 30°/30° iso follow-cam that holds the last square on a fall, reflective PBR marbles (steel/glass/rubber/…) under IBL with 1 m grouted tiles, mechanisms (bumper/spring/elevator/arm), countdown HUD + delayed respawn. Generic engine additions landed in `OpenGLContext` with tests: `apply_impulse`/`apply_angular_impulse`, `set_pair_friction`, `physics.KinematicAnimator`, `move.followcam`, and a `renderShaderOverlay` FlatPass hook. Ongoing visual polish

<a name="code-review-2026-07-26"></a>
### [CODE-REVIEW-2026-07-26.md](CODE-REVIEW-2026-07-26.md) — ✅ Complete

Critical review of all changes since `8e7703a` (12 commits, +32.5 k): the overlay UI (`ui/`), declared movement modes, the `renderoptions`/`ContextDefinition` settings indirection, the input sampler, teapot instancing and the test-suite expansion. 45 findings, each with a reproduction and a fix, in a summary table with per-finding state. **All 45 fixed (2026-07-26)**, every one Red/Green; full unit suite green (3329 passed, 0 failed). Highlights: the mypy gate made runnable and the `warn_return_any` override narrowed back to `physics.*` (24 real errors fixed, `FontMetrics` typed through the layout instead of `Any`); **one root cause behind three findings** — an environment variable read before anything can set it — as a memo with no reset, a class attribute assigned at import, and `ContextDefinition.profile` frozen at class-definition time, the last of which was silently rendering the glTF conformance suite in the wrong profile; `Slider` hit-testing the geometry it paints and keeping a stable track; a `NumberField` so a generated numeric editor no longer raises on the first keystroke; a bindable `KeyCapture`; `OverlayStack.clear` no longer able to hang; the console following the tail only for a reader at the tail and its log handler letting the panel go; modifiers sampled live rather than remembered per key; key bindings saved atomically and only on Save, with Cancel restoring; a public `set_cull_state` replacing a private cross-package protocol; `NavigationManager.retarget` keeping the player's chosen mode

<a name="core-profile-compatibility"></a>
### [CORE-PROFILE-COMPATIBILITY.md](CORE-PROFILE-COMPATIBILITY.md) — ✅ Complete

Make flatcore.py actually core-profile compatible. Delivered via [SHADER-BASED-VRML97.md](SHADER-BASED-VRML97.md): all listed geometry/background/text nodes have `mode.shader_mode` render paths (box/quadrics/indexedlineset/pointset/gear/arraygeometry; IFS compiles to ArrayGeometry); flatcore/_flat push matrices as shader uniforms and gate every fixed-function `glMatrixMode`/`glLoadMatrixf`/`GL_LIGHTING` call behind `if use_shaders: … else: <legacy>` so none run under core. Lighting/material intentionally applied via uniform helpers (`configure_light_from_node`/`configure_material_from_node`) rather than rewriting light.py/material.py/appearance.py. Core tested via the conftest `PROFILES=['compatibility','core']` fixture + `run_core_tests.py` + `test_core_pass_dispatch.py` (the originally-proposed `tests/test_core_profile.py`/`tests/core/` scaffolding was superseded by these)

<a name="selection-optimization"></a>
### [SELECTION-OPTIMIZATION.md](SELECTION-OPTIMIZATION.md) — ✅ Complete

MRT object-ID selection shipped (refactored into `passes/selection.py`): `SelectionBufferFBO` (color+id+depth MRT), `processPickEventsFromBuffer` O(1) pick lookup, `SelectionMixin`/`use_mrt_selection`, wired into the forward pass; all fragment shaders emit `layout(location=1)` object IDs; `set_object_id` in shaderpass.py (program-switch-safe). **Phase 3.2 async PBO readback fully implemented** (pooled pixel-pack buffers + `glFenceSync` + `glMapBufferRange`, ≤4 in-flight), not just noted. Tests: `test_mrt_draw_buffer_contract`, `test_async_pick_gl`, `test_click_through_gl`, etc. Only Phase 3.1 (BVH hierarchical selection) unimplemented — plan marks it optional/future

<a name="instanced-geometry"></a>
### [INSTANCED-GEOMETRY.md](INSTANCED-GEOMETRY.md) — ✅ Complete

Collapse shapes sharing one geometry into a single `glDrawElementsInstanced` (sphere fields, repeated parts), attacking the O(N) draw-loop wall. **Engine + 5 staged draw paths shipped + tested** (~2.1x faster, N draws→1, every instance still pickable): (1) PBR per-instance model+id, (2) per-instance material-array factors + UBO chunking, (3) opportunistic content-hash collapse of distinct-node identical meshes, (4) glTF `EXT_mesh_gpu_instancing` + shared-mesh, (5) VRML97 lit shader instancing. **Geometry coverage:** primitives + PBRMesh + **IndexedFaceSet** (general triangle mesh) instance; Teapot/NURBS/Extrusion deliberately not (would lose distance-LOD). **Performance:** per-frame VAO/VBO/material-UBO churn now cached; **cluster culling** added (`OPENGLCONTEXT_INSTANCE_CLUSTER_CULL=1`) — after finding off-screen instances were already culled per-object, so this only lowers per-frame CPU cull cost. User docs: [docs/instancing.html]. Env toggles `OPENGLCONTEXT_INSTANCING`/`_INSTANCE_COLLAPSE`/`_INSTANCE_MIN`/`_INSTANCE_CLUSTER_CULL`. **Optional/deferred (not required):** Text glyph instancing, PointSet repeat, and hardware-gated fast paths (SSBO material array, `glMultiDrawElementsIndirect`, bindless per-instance textures — detected, scoped in the plan)

<a name="movement-modes"></a>
### [MOVEMENT-MODES.md](MOVEMENT-MODES.md) — ✅ Complete

Walking, flying, swimming and first-person mouse-look as **declared `PROTO` nodes** with their own typed fields, replacing per-application key bookkeeping. Input is *sampled* once per frame (`events/inputstate.py`) rather than reacted to, so several inputs act together — walking and jumping in one frame needs no special case. `enter_when()` lets the world impose a mode (swimming); `KeyBinding` carries an optional modifier so ctrl+arrow tilts the view without also walking; `NavigationManager.binding_table()`/`rebind()` are what a settings screen will drive. The context publishes the mode in force on `ContextDefinition.movementMode` for watchers, `getNavigationPlatform()` is the seam for driving a character controller instead of the camera, and a mode with `capturePointer` grabs the pointer (GLFW disabled cursor + raw motion), released while an overlay wants it. Wired into `vrml_view`, `gltf_view` and the twig-bb map viewer. User docs: [docs/navigation.html]

<a name="overlay-ui"></a>
### [OVERLAY-UI.md](OVERLAY-UI.md) — ✅ Complete

In-window overlay UI: settings, key bindings, console and licence text over the running world. Widgets (label/button/toggle/select/slider/text/key-capture), Row/Column/Grid boxes, a scrolling viewport with a scrollbar, nine-slice artwork per widget state, translucent by default. **The model is a node and widgets edit a copy** — `SettingsSession.draft`/`commit()`/`revert()`, nested one level per sub-page — so validation, defaults, serialisation and change notification come from the field system and Cancel is real at every level. A modal panel sinks *all* input including the movement sampler; a `capturing` one passes Tab/Enter/mouse verbatim for rebinding, with Escape reserved. Every hidden rendering feature (shadows, soft shadows, cascades, light limit, bloom, IBL + intensity, transmission, instancing, distance LOD, vsync) is now a `ContextDefinition` field read through `renderoptions`, with the env var as its default, and the settings page is generated from those fields. **The whole interface scales with the window** (`ui.metrics.font_size_for` picks an atlas from the window height, `uiScale` is the player's multiplier on top, and every pixel measurement — skin insets, widget margins, `maximumWidth`, grid row padding — goes through the scale that comes back); booleans are sliding switches drawn from a generated antialiased disc rather than check boxes; `preferredColumns` is a panel's **maximum width** so a screen is a centred column on a 4K display; generated rows carry padding, a hairline rule and an active-row wash. Docs: [docs/overlayui.html](../docs/overlayui.html); demo: `oglc-ui [--skin]`

<a name="hud-debug-overlay"></a>
### [HUD-DEBUG-OVERLAY.md](HUD-DEBUG-OVERLAY.md) — ✅ Complete

Screen furniture drawn over a live world, and the developer overlay that replaces the frame-rate display. **A HUD is not a panel**: the overlay stack is modal, which is right for a settings screen and wrong for a health bar, so a `HUDLayer` is a widget tree that fills the viewport, is laid out every frame, answers `widget_at()` with `None` and is drawn *under* any panel that is open. Placement is by **anchor** (`top-left` … `bottom-right`) plus an offset, because no row/column arithmetic describes “health in that corner, reticule in the middle” as well as naming the corner does; a child with no anchor still gets the whole layer and lays itself out as before. Widgets: `Crosshair` (**the reticule is a weapon's property** — shape, gap, arm length, thickness and a `spread` that widens with the weapon's accuracy, plus a hit mark), `BarMeter` (three threshold colours rather than a gradient, because a player reads a state and not a number), `Readout` (icon + number) and `MessageQueue` (newest first, each line on its own clock, fading in its alpha). **The developer overlay is fed by registered providers** — a callable returning name/value pairs — so a subsystem appears in it by registering rather than by anyone editing it; values are formatted centrally, a provider that raises becomes an `error` row instead of taking the frame down, and one with nothing to say is left out rather than drawn as an empty heading. Shipped providers: frame rate/time/viewport, renderer features + `passes/renderstats.py` frame counts (shapes, draws, instanced groups — **counted where the pass knows the answer; triangle counts deliberately absent rather than guessed**), camera and character-controller state, and a physics body/contact provider. **Retired**: `FrameCounter.Render` (fixed-function `glOrtho`/`glPushAttrib` in a core-profile renderer), its shader-side duplicate `shaderRenderFrameCounter`, and `FrameCounter.display`; Alt+F now toggles the overlay and `OPENGLCONTEXT_DISABLE_FPS_DISPLAY` decides whether it *starts* hidden, so the capture harness needed no change. `ScreenMixin` is a base of `Context` itself (every context has a frame rate) and `OverlayMixin` extends it, so HUD and panels are one list drawn in one batch. `Panel`'s children/skin/scaling/link machinery moved up into `widgets.RootWidget`, which a panel and a HUD layer are both kinds of. **Red/Green TDD: 93 tests with no GL** (anchors, reticule geometry, thresholds, message expiry, provider registration/ordering/formatting, laid-out rows, drawing order) **+ 7 against a real driver**. Docs: [docs/hud.html](../docs/hud.html). First customer: twig-bb's `twig_bb.hud`

<a name="loop-instrumentation"></a>
### [LOOP-INSTRUMENTATION.md](LOOP-INSTRUMENTATION.md) — ✅ Complete

Measuring the thing the frame rate is not. **A game can report 50 fps while moving once a second, with every number on screen correct**: `FrameCounter` times only the inside of `OnDraw` (so `poll_events`, `OnIdle` — where an application’s whole simulation usually lives — and the redraw wait are invisible), it is only told about frames that produced a *visible change*, and the rate it publishes is a **median**, deliberately outlier-proof so a shader compile cannot drag it down for ever. Each choice right; together, blind to a stutter. `looptrace.LoopTrace` measures **wall-clock time per loop iteration divided among named phases**, where **a phase is charged only its own time and a nested phase’s belongs to the child** — so the phases of an iteration *sum* to its wall clock and the largest is the culprit by construction, not by comparison. Keeps what the median throws away: `worst_ms`, a `stalls` count that outlives the window it happened in, and the phase breakdown of the last stall. New **Loop** section sits directly under Frame because the two are read *against each other* — a healthy `fps` over a poor `loop fps` is the whole diagnosis. `GLFWContext.MainLoop` opens `poll`/`repeats`/`idle`/`wait`/`draw`; `OnDraw` subdivides into `cascade`/`render`; `Context.tracePhase()` answers a do-nothing manager so callers write the `with` and not the branch; a backend that never opens an iteration gets no section rather than rows of zeroes. Also: `ThreadedSimulation.rate()`/`.dropped` + `simulation_provider` (**a starved physics thread and wrong gravity look identical on screen** until `sim hz` sits beside `asked`), and twig-bb’s `FrameClock`, which keeps the accounting the timestep clamp destroys — `real`, `lost`, cumulative `debt` and `pace`, so “5% speed, 0.9s lost” says outright that the world is running at a twentieth of speed. `OPENGLCONTEXT_STALL_MS` / `OPENGLCONTEXT_TRACE_STALLS` log a stall’s breakdown; counting is always on and costs a few clock reads. **Red/Green TDD: 65 tests**, the load-bearing one being 30 frames at 20ms + one at a second + 30 more — median 20ms, worst 1000ms. Docs: [docs/hud.html](../docs/hud.html)

<a name="performance-shadow-regression-2026-07"></a>
### [PERFORMANCE-SHADOW-REGRESSION-2026-07.md](PERFORMANCE-SHADOW-REGRESSION-2026-07.md) — ✅ Complete

Parthenon dropped 60→20 fps. py-spy: the PyOpenGL glx/egl context probe is exonerated (0.02 %); the cost was `renderShadowMaps` (**68 %** of frame) re-culling/re-drawing all 287 casters every frame with **no caching**. **Shipped R1** (cache caster world-points/AABB), **R2** (reuse spot/point depth maps while static), **R3** (cull once per directional light), **R4** (hoist instance grouping out of the per-cascade loop + vectorize modelview/buffer packing), **R5** (cache the grouping across frames/lights — reframed from a loader merge after profiling showed instancing already collapses the 287 casters to 29 draws). Walk-case shadow pass **22.4→6.2 ms (−72 %)**, spin **22.0→12.5 ms (−43 %)**, pixel-identical, 20 new TDD tests, 181-test regression sweep green. Remaining (future): vectorize `_cullOccluders`/`_extend_near_for_casters`; a real loader merge would help only the animated case

<a name="pbr-brdf-conformance"></a>
### [PBR-BRDF-CONFORMANCE.md](PBR-BRDF-CONFORMANCE.md) — ✅ Complete

Term-by-term audit of the PBR uber-shader **and all 19 supported glTF extensions** against Appendix B + each `KHR_*/EXT_*` spec + the Khronos reference renderer (6 parallel agent audits), **then the full punch-list was implemented**. **Core metallic-roughness BRDF is conformant** (D_GGX/height-correlated Smith V/Schlick F/Lambert identical to the reference; `α=roughness²`, 0.04 F0, `(1−F)(1−metallic)` energy split). Fixes landed (Tier 1/2/3): spotlight 2×-cone corrected (loader stores authored half-angles; shadow projection uses full-cone FOV); `node_visibility` propagates to children; sheen ported `V_Sheen`+`D_Charlie` with a 3rd BRDF-LUT channel for albedo-scaling and IBL sheen; specular carries `specularFactor` as f90 (direct+IBL+transmission) with specular/specularColor textures; iridescence `irSensitivity` takes a vec3 shift + `schlickToF0` kills the double-Fresnel; clearcoat uses the geometric/`clearcoatNormalTexture` coat normal; volume thickness scaled by model matrix + diffuse-transmission absorption tint; transmission `(1−specular)` energy term + IOR→mip + `KHR_materials_dispersion`; diffuse-transmission albedo double-tint removed, ambient de-energized, −N IBL back-glow; range-window applied once + spot falloff `t²`; `KHR_animation_pointer` implemented LIVE (Player-driven setters); `KHR_materials_anisotropy`; gpu-instancing normalized-int ROTATION dequantized; texture-transform `texCoord` override honoured. **Only deliberate exclusion:** `KHR_texture_basisu` (no Basis transcoder available; no demo model uses it, so unexercised). Overlapped P1/P6 in GLTF-FULL-FEATURE-CONFORMANCE + iridescence in GLTF-DEMO-CONFORMANCE

<a name="gltf-complete-support"></a>
### [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md) — ✅ Complete

Close the gap to Khronos reference renders: catalogued from an 88-model QA pass, now **implemented** — animation, skinning, morph targets, real IBL (`OPENGLCONTEXT_IBL`), sampler/wrap+mipmaps, alpha blend + screen-space transmission, second UV `TEXCOORD_1`, sparse accessors, non-triangle primitives, computed tangents, `KHR_lights_punctual`, and the full `KHR_materials_*`/`KHR_texture_transform`/unlit/specGloss family (delivered with PBR-BRDF-CONFORMANCE). Background/async load + camera auto-framing landed. `KHR_draco_mesh_compression` now decodes via the optional `DracoPy` library (tracked in [DRACO-COMPRESSION.md](DRACO-COMPRESSION.md))

<a name="draco-compression"></a>
### [DRACO-COMPRESSION.md](DRACO-COMPRESSION.md) — ✅ Complete

Decode `KHR_draco_mesh_compression` glTF geometry using the optional `DracoPy` library when installed (soft dependency: `loaders/gltf/draco.py` decodes Draco only when present, warn-and-skip otherwise). Draco-compressed `.glb`/`.gltf` assets now load, verified against the Khronos Duck Draco variant (counts/bounds match the uncompressed model) with unit, missing-library, and render tests in `tests/test_gltf_draco.py`. Ties into [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md) / [GLTF-FULL-FEATURE-CONFORMANCE.md](GLTF-FULL-FEATURE-CONFORMANCE.md)

<a name="gltf-demo-conformance"></a>
### [GLTF-DEMO-CONFORMANCE.md](GLTF-DEMO-CONFORMANCE.md) — ✅ Complete

QA/acceptance track for **100% reference parity** on the Khronos glTF-Sample-Models. **Feature/triage catalogue resolved** (iridescence, second UV, per-channel texture-transform, transmission/clearcoat textures, NegativeScale normals, emissive bloom/HDR, specular>1/EnvironmentTest env-cubemap IBL). **Acceptance layer delivered:** [tests/test_gltf_conformance.py](../../tests/test_gltf_conformance.py) gates **every** Khronos sample — roster expanded 122→**149 scenes** (all 148 catalogue models + Parthenon, 158 views), each rendered and diffed against a blessed baseline (`reference-images/gltf_baseline`) reusing `oglc-gltf-regression`'s `render_view`/`compare` + shared `ComparisonResult`; fast no-GL baseline-or-waived anchor + `slow`/`visual` render gate. **Two waived** (feature unimplemented): ScatteringSkull (subsurface) + USDShaderBallForGltf (nested-shell transmission)

<a name="teapot-uv-coordinates"></a>
### Teapot UV coordinates — ✅ Complete

Per-vertex UV + PBR on the Teapot. Surfaced the GLU tessellator's parametric (u,v) via an opt-in `GL_MAP2_TEXTURE_COORD_2` map + texture-coord callback (`nurbs.py`), interleaved as **T2F_N3F_V3F** (texcoord→`aTexCoord` loc 0, not loc 2 as first assumed). Implements the Utah **"injective, with interior"** layout (graphics.cs.utah.edu/teapot): each Bézier patch maps to a non-overlapping atlas region, and each patch also emits reversed-winding, negated-normal **interior faces** into interior regions, rendered with **backface culling** (exterior outside / textured interior through the mouth, no z-fighting). Teapot also emits **tangents** (loc 3, `compute_tangents`) so the PBR normal/bump map works. **First PBR material on non-glTF geometry:** `tests/teapot_ceramic.py` applies a `PBRMaterial` (procedural celadon glaze + *arare* bump, `tests/_ceramic_textures.py`) to the plain `Teapot` Shape — no `Shape` change needed (its existing PBR branch drives it once the geometry supplies texcoords/tangents). Works in core + compat profiles. New injective-layout/interior + non-glTF-PBR tests (`test_teapot_nurbs.py`, `test_pbr_rendering.py`). Caveat: injective layout is ~10× anisotropic on the body, so the bump is kept shallow (reads as throwing-rings there)

<a name="terrain-asset-formats"></a>
### [TERRAIN-ASSET-FORMATS.md](TERRAIN-ASSET-FORMATS.md) — ✅ Complete

Reference: input asset formats for `SplatTerrain`/`HeightField` + provenance of the forest demo's terrain. Documents (1) the **heightmap** — a 16-bit grayscale PNG made from **AWS Terrain Tiles (Terrarium)** for the Great Smoky Mountains (lat 35.60, lon −83.50, z12, 4×4→1024²) via `elev = R·256+G+B/256−32768`, normalised; loaded by `HeightField.from_image` (`/65535 → 0..1`, ×`relief` over an `extent` square, world↔texel mapping); (2) the **RGBA splat control map** — one channel per detail layer (R=forest_floor,G=grass,B=rock,A=moss), sampled by world XZ and normalised in `terrain_splat.frag`, generated from the DEM's slope+elevation; (3) the CC0 detail-layer material arrays. Includes the complete runnable fetch/decode + control-build recipes (were ad-hoc scripts, not a committed tool)

<a name="animated-pbr-surfaces"></a>
### Animated PBR surfaces — ✅ Complete

A **surface deformer** on `PBRMesh`: a per-frame callable a *material* uses to move the geometry it is painted on, as distinct from morph targets and skinning, which are the mesh's own animation. It rides the same pipeline — rest arrays, the morph→skin→surface chain, the version counter, the dynamic VBO re-upload — and adds the two properties that make it safe: the deformer always sees the **rest pose** (deforming its own last output compounds, and a one-unit wave walks the surface away in seconds) and is handed **copies** (so a deformer that writes in place cannot corrupt what it is given next frame). Texture coordinates join the dynamic buffers **only** when the mesh says its UVs move (`deforms_texcoords`), so a skinned character does not pay to re-upload UVs that never change. Together with the `uv_transform`/`texCoordMask` and material-version seams that already existed for `KHR_texture_transform`, this is what lets Quake 3 `.shader` animation — `tcMod`, `rgbGen`, `alphaGen`, `animMap`, `deformVertexes` — be drawn: see [twig-bb](../../twig-bb/README.md#surfaces-that-move), whose `animator.py` is the worked example. **Red/Green TDD: 22 tests**, no GL. Docs: [docs/pbr.html#animated-surfaces](../docs/pbr.html)

<a name="spatial-audio"></a>
### [SPATIAL-AUDIO.md](SPATIAL-AUDIO.md) — ✅ Complete

Sound placed **in** the scene, on glTF's `KHR_audio_emitter` model (the Web Audio `PannerNode` model) rather than a private format — the same decision physics made with OMI, so a scene authored in Blender or Godot arrives with its sound intact. The engine ships as the standalone **`omi_audio`** package (model/spatial/clip/synth/mixer/device/engine — numpy only, no renderer in it); OpenGLContext keeps the integrations: `scenegraph/audio.py` (`AudioEmitter`/`AudioSource`, and VRML97's long-declared `Sound`/`AudioClip` finally audible on their own ellipsoid curve) and `OpenGLContext/audio/` (scene/settings). Three distance models, a Web Audio cone, equal-power panning about the camera-as-listener, and an underwater low-pass. The mixer is **ours, in numpy**, so no copyleft audio library is a dependency: a fixed voice pool with priority stealing, per-block gain ramping, and a hard no-allocation/no-lock/no-log rule on the audio thread (asserted by a tracemalloc test). `play()` hands out a generation-checked handle so a stolen voice's old owner steers nothing rather than somebody else's explosion. **Silence is a backend**: package absent, device won't open, or the backend's own null output — one warning and a `NullDevice`, and `open_device()` cannot raise. Sound is driven from `FlatPass.beginFrame` (not `Render`, which is overridden per profile) and costs nothing until a scene has an audible node. `miniaudio` is the only audio dependency and is **optional** because it ships no `manylinux_aarch64` wheel. Player settings live in a `ContextDefinition.audio` sub-node and generate their own settings section; the application's `master_gain` multiplies with the player's `volume` rather than being overwritten by it. **Red/Green TDD: 336 tests**, split with the code (`omi_audio`'s own suite plus OpenGLContext's `test_audio_{nodes,scene,gltf,pass_wiring,demo_audible}`), including two written against defects found only by listening — one asserting the level in dBFS at the *device boundary* for a source at a stated position, one holding the demo's own scene to a comfortable band. Docs: [docs/audio.html](../docs/audio.html) with data-flow diagrams; demo `tests/audio_spatial.py`

<a name="particle-effects"></a>
### [PARTICLE-EFFECTS.md](PARTICLE-EFFECTS.md) — ✅ Complete

Fire, smoke, sparks, explosions, impacts and trails as camera-facing quads — **one instanced draw per system**, however many particles. `scenegraph/particles.py`: a `ParticlePool` of numpy arrays with **the living packed at the front** (survivors compact over the dead, so the live set is one contiguous slice and the upload is one `glBufferSubData`), and a `ParticleEmitter` rendering node with declared fields and `UI_HINTS`. **Per particle is state, per system is a curve**: seven floats per instance, with colour and size over life as two uniforms the shader lerps — so editing `size` resizes what is already in the air. Billboarding in eye space (exact at any camera orientation, no per-particle matrix); the node picks view vs model-view so the shader has no branch. Depth tested, depth writes off, additive by default, and a soft round dot computed in the fragment shader when no texture is given — so an effect needs no asset. Six presets. Careful about the things that bite: exponential drag (linear reverses on a long frame), a clamped frame time, a fractional emission accumulator, and a cone that samples the spherical cap rather than the angle. **Red/Green TDD: 71 tests**, no GL, including an allocation assertion. Docs: [docs/particles.html](../docs/particles.html); demo `tests/particles_effects.py`

<a name="fog-and-swimming"></a>
### Fog, and swimming — ✅ Complete

Two nodes that had been declared and never driven, finally driven. **VRML97's `Fog`** (`scenegraph/fog.py`) is rendered through the PBR shader: the pass has collected `nodetypes.Fog` paths for years and nothing read them, and a dangling `context.gltf_fog` seam that nothing ever set is retired in its favour. A node rather than a number *because fog is a property of a place* — it binds and stacks like a viewpoint, so being under water, in smoke and in clear air are three fogs in one scene with one in force. **Both of the specification's curves**, because they are not approximations of each other: `LINEAR` fades from the first metre, `EXPONENTIAL` hangs back and closes in, and the difference is whether the weapon in the player's hands is tinted as much as the far wall — which is exactly what separates *being inside a medium* from a coloured pane laid over the screen. One uniform (the reciprocal of the visible range) serves every mode, the blend happens in linear HDR before tone mapping, and a scene with no fog stays pixel-identical. **Swimming** stops being a use of noclip: `MovementMode.applyTo(platform)` is where a mode puts the *body* into the state its movement assumes, `SwimMode` asks for `set_swim(buoyancy=...)` rather than `set_fly(True)`, and omi_physics' `CharacterController` grew a swim step — flying's movement, walking's collision, and a vertical that is neither: gravity scaled by what buoyancy does not cancel, bled off by drag, so a sink settles at a steady speed instead of accelerating. A swim built on noclip is a player who can leave a pool through its wall. **Red/Green TDD: 14 fog-node tests + 5 against a real driver, 5 mode tests, 21 swim tests.** Docs: [docs/pbr.html](../docs/pbr.html) §Fog and [docs/navigation.html](../docs/navigation.html). First customer: twig-bb's `twig_bb.underwater`

<a name="physics-collision"></a>
### [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md) — ✅ Complete (all 8 phases, 0–7)

Real-time game-like rigid-body physics beside the scenegraph, **built natively on the OMI glTF physics model** (`OMI_physics_shape`/`_body`/`_gravity`/`_joint`). **Shipped** as the `OpenGLContext/physics/` package (model/world/backend/body/collide/gjk/broadphase/narrowphase/solver/hull/cookery/gravity/triggers/joints/character/omi_gltf/debugdraw/manager/demo) + `scenegraph/physicsbody.py` + `move/physicsplatform.py` + `bin/physics_cook.py`. Symplectic-Euler + fixed-timestep accumulator + render interpolation, global+volume gravity (priority/replace/stop), linear+quadratic drag, materials (friction/restitution + combine modes), collision filters, dynamic AABB-tree broad phase, analytic + **GJK/EPA** narrow phase, sequential-impulse contact+joint solver (warm-start + split-impulse position correction, contact **islands**, **sleeping**), triggers (enter/stay/exit, no impulse). Mesh↔mesh via OMI `convex` (own QuickHull + approximate convex decomposition, no scipy) + static `trimesh` triangle-soup; **cooking** (`cook_shape`) + `physics-cook` bake CLI. `PhysicsBody` syncs `Transform`; `PhysicsViewPlatform` (non-OMI `CharacterCapabilities`: walk/run/sprint/crouch/jump/fly, step/slope) with **safe viewpoint binding** (depenetration + ground-snap, never stuck). SoA columnar layout + kernel-shaped stages + `backend.py` seam are **GPGPU-accelerated**: `GLComputeBackend` (`glcompute.py`) runs per-body force/position integration as **GL 4.3 compute shaders** (fused single dispatch for collision-free steps), ~2.4× over numpy at 10⁵ movers; the default `auto` policy stays on numpy below `gpu_threshold` (10k) awake bodies and hands off to the GPU above it (hysteresis + numpy fallback; broad/narrow/solve stay CPU; full GPU-resident loop future). **Debug overlay** renders proxies/AABBs/contacts + per-body **velocity/acceleration/angular-velocity** (corner-spin) vectors, sleep state and joint links. **Red/Green TDD throughout: 91 tests green** across 18 modules; **9 interactive demos** (`tests/physics_*.py`) double as visual-regression tests (verified via the auto-exit/capture harness); `docs/physics.html` + getting-started tutorial, linked from the docs index. Scale = import-time only

## Recent Progress (January 2026)

### Test Suite Implementation

The automated test suite is now functional with the following capabilities:

- **118 visual test scripts** run with screenshot capture and comparison
- **129 total test scripts** including non-visual functionality tests
- **HTML report generation** at `tests/report.html` with side-by-side image comparisons
- **Coverage collection** from subprocess test runs (currently ~9% coverage)
- **Auto-exit mechanism** via `OPENGLCONTEXT_AUTO_EXIT_FRAMES` environment variable
- **Screenshot capture** via `OPENGLCONTEXT_AUTO_EXIT_CAPTURE_DIR` and `OPENGLCONTEXT_AUTO_EXIT_CAPTURE_NAME`
- **FPS display toggle** via `OPENGLCONTEXT_DISABLE_FPS_DISPLAY` for clean screenshots

Script categorization:

- Platform-specific scripts (Windows/WGL, wxPython, pygame) with automatic skip logic
- Non-visual scripts (glget.py, boundingvolume.py, etc.) excluded from visual regression
- Randomized scripts (particles, starfield) marked with `expect_visual_diff`

Test results from last run: 247 passed, 7 failed, 6 skipped

### Known Issues

- `solid_font.py` - Font provider registry issue (`fontNameFromStyle` on None)
- `glutmousewheel.py` - GLUT-specific test failing
- `savepostscript.py` - PostScript save functionality
- `shader_instanced*.py` - Legacy GLSL compatibility issues on some drivers
