# OpenGLContext Project Plan

## Summary

**Status legend:** ✅ **Complete** — shipped, nothing required outstanding · 🟡 **Partial** — core landed but named pieces still missing · 📋 **Planned** — has a plan document, not yet built · ⬜ **Todo** — wanted, no plan document yet · 🛑 **Shelved** — deliberately deferred.

| Plan | Status | Description |
|------|--------|-------------|
| [MARBLE-MADNESS-DEMO.md](MARBLE-MADNESS-DEMO.md) | ✅ Complete | `marble-demo` scene package: an isometric Marble Madness re-interpretation, **built through all 7 phases with Red/Green TDD (77 tests green), playable via `oglc-marble`**. Procedurally generated tilted tracks (surface patches, ramps, rails), pure-spin arrow-key steering with a pairwise marble↔surface friction grid, real ramp jumps, 30°/30° iso follow-cam that holds the last square on a fall, reflective PBR marbles (steel/glass/rubber/…) under IBL with 1 m grouted tiles, mechanisms (bumper/spring/elevator/arm), countdown HUD + delayed respawn. Generic engine additions landed in `OpenGLContext` with tests: `apply_impulse`/`apply_angular_impulse`, `set_pair_friction`, `physics.KinematicAnimator`, `move.followcam`, and a `renderShaderOverlay` FlatPass hook. Ongoing visual polish |
| [CODE-REVIEW-2026-07-19.md](CODE-REVIEW-2026-07-19.md) | 🟡 Partial | Critical review of all changes since `9eb2ff1` (terrain/vegetation/IBL/shadow-caching/tiles3d + forest-demo). 15 ranked + ~40 per-subsystem findings, each with a status. **Pass 1 (2026-07-19):** ranked 1–4, 6–7, 9, 11–15 (IBL `_build` exception-safety, both shadow cache bugs, `dispose()` + retained handles, `ensure_gl` fallback, GL-baseline doc, `castsShadow` depth filter, heightfield bit-depth, in-memory clump texture, integer scatter hash, local GL save/restore, terrainwalk broadphase). **Pass 2 (2026-07-20):** V1/V4–V7, T1/T3–T7, I1/I4–I8, S2/S4, G1–G6, X1/X2/X4/X5, D1–D3 — vegetation `InstancedVegBase` dedup + shared `LOD_NEAR/FAR` uniform + `InstanceBuffer`, world-up ambient, IBL float-render capability probe + LDR skybox fallback + shared `dirToEquirect` include + split `_build`, Draco hardening, `cc0`→`loaders/`, and the **forest-demo** made importable/reusable (`ForestConfig`+`build_forest_scene`→`ForestScene`, argparse CLI, bundled clump asset) for a future driving demo. All with tests; 388 affected + 403/4-xfail render + 8 demo tests green. **Still open: ranked 5** (Draco decode-size cap), **8** (sampler-budget unification), **10** (prune duplicate tiles3d vegetation); lower-sev V2/T2/I3/X3, S1 partial |
| [CODE-REVIEW-2026-01.md](CODE-REVIEW-2026-01.md) | ✅ Complete | Comprehensive code review with recommendations |
| [GOD-OBJECT-DECOMPOSITION.md](GOD-OBJECT-DECOMPOSITION.md) | ✅ Complete | Behaviour-preserving split of the 8 god objects flagged in CODE-REVIEW-2026-07-13 §3n/§7c (selection, indexedfaceset, nurbs, shaderpass, context, gltf, shadowmixin, _flat) into cohesive sibling modules/mixins; every import path unchanged, 340+ tests + off-screen renders green |
| [PYTHON-MODERNIZATION.md](PYTHON-MODERNIZATION.md) | ✅ Complete | Modernize to require Python 3.10+ and remove Python 2.7 compatibility |
| [GPU-NURBS-TESSELLATION.md](GPU-NURBS-TESSELLATION.md) | 📋 Planned | GPU-based NURBS rendering using tessellation shaders |
| [SHADER-BASED-VRML97.md](SHADER-BASED-VRML97.md) | ✅ Complete | Shader-based rendering pass implementing VRML97 lighting model |
| [CORE-PROFILE-COMPATIBILITY.md](CORE-PROFILE-COMPATIBILITY.md) | ✅ Complete | Make flatcore.py actually core-profile compatible. Delivered via [SHADER-BASED-VRML97.md](SHADER-BASED-VRML97.md): all listed geometry/background/text nodes have `mode.shader_mode` render paths (box/quadrics/indexedlineset/pointset/gear/arraygeometry; IFS compiles to ArrayGeometry); flatcore/_flat push matrices as shader uniforms and gate every fixed-function `glMatrixMode`/`glLoadMatrixf`/`GL_LIGHTING` call behind `if use_shaders: … else: <legacy>` so none run under core. Lighting/material intentionally applied via uniform helpers (`configure_light_from_node`/`configure_material_from_node`) rather than rewriting light.py/material.py/appearance.py. Core tested via the conftest `PROFILES=['compatibility','core']` fixture + `run_core_tests.py` + `test_core_pass_dispatch.py` (the originally-proposed `tests/test_core_profile.py`/`tests/core/` scaffolding was superseded by these) |
| [SELECTION-OPTIMIZATION.md](SELECTION-OPTIMIZATION.md) | ✅ Complete | MRT object-ID selection shipped (refactored into `passes/selection.py`): `SelectionBufferFBO` (color+id+depth MRT), `processPickEventsFromBuffer` O(1) pick lookup, `SelectionMixin`/`use_mrt_selection`, wired into the forward pass; all fragment shaders emit `layout(location=1)` object IDs; `set_object_id` in shaderpass.py (program-switch-safe). **Phase 3.2 async PBO readback fully implemented** (pooled pixel-pack buffers + `glFenceSync` + `glMapBufferRange`, ≤4 in-flight), not just noted. Tests: `test_mrt_draw_buffer_contract`, `test_async_pick_gl`, `test_click_through_gl`, etc. Only Phase 3.1 (BVH hierarchical selection) unimplemented — plan marks it optional/future |
| [RAYCAST-PICKING.md](RAYCAST-PICKING.md) | 🛑 Shelved | CPU ray-cast picking for large scenes: cached world BVH broad phase + geometry-delegated `intersect()` narrow phase (analytic primitives, triangle-BVH meshes, point/line tolerance); MRT/`GL_SELECT` buffer kept as fallback. Shelved — async MRT buffer path is enough (~0–10% overhead, O(1)/pick); real wall is the O(N) draw loop, addressed by instancing |
| [INSTANCED-GEOMETRY.md](INSTANCED-GEOMETRY.md) | ✅ Complete | Collapse shapes sharing one geometry into a single `glDrawElementsInstanced` (sphere fields, repeated parts), attacking the O(N) draw-loop wall. **Engine + 5 staged draw paths shipped + tested** (~2.1x faster, N draws→1, every instance still pickable): (1) PBR per-instance model+id, (2) per-instance material-array factors + UBO chunking, (3) opportunistic content-hash collapse of distinct-node identical meshes, (4) glTF `EXT_mesh_gpu_instancing` + shared-mesh, (5) VRML97 lit shader instancing. **Geometry coverage:** primitives + PBRMesh + **IndexedFaceSet** (general triangle mesh) instance; Teapot/NURBS/Extrusion deliberately not (would lose distance-LOD). **Performance:** per-frame VAO/VBO/material-UBO churn now cached; **cluster culling** added (`OPENGLCONTEXT_INSTANCE_CLUSTER_CULL=1`) — after finding off-screen instances were already culled per-object, so this only lowers per-frame CPU cull cost. User docs: [docs/instancing.html]. Env toggles `OPENGLCONTEXT_INSTANCING`/`_INSTANCE_COLLAPSE`/`_INSTANCE_MIN`/`_INSTANCE_CLUSTER_CULL`. **Optional/deferred (not required):** Text glyph instancing, PointSet repeat, and hardware-gated fast paths (SSBO material array, `glMultiDrawElementsIndirect`, bindless per-instance textures — detected, scoped in the plan) |
| Click-through + unlit picking | ✅ Complete | `Shape.pickable=False` masks the object-id MRT attachment so picks read through (water/gizmo/annotation). Also made unlit geometry pickable (PBR `KHR_materials_unlit` + `vrml97_unlit`, object-id follows program switches) and fixed a finding-4.6 blend bug zeroing point ids. 12 new tests |
| [ORDER-INDEPENDENT-TRANSPARENCY.md](ORDER-INDEPENDENT-TRANSPARENCY.md) | 📋 Planned | Implement order-independent transparency to eliminate sorting artifacts |
| [SHADOW-MAPPING.md](SHADOW-MAPPING.md) | ✅ Complete | High-quality shadow mapping in the VRML97 shader: spot (2D), directional (CSM), point (omni cube), PCF + PCSS soft shadows, range culling. 58 tests. VSM/EVSM deferred (documented reason) |
| [PERFORMANCE-SHADOW-REGRESSION-2026-07.md](PERFORMANCE-SHADOW-REGRESSION-2026-07.md) | ✅ Complete | Parthenon dropped 60→20 fps. py-spy: the PyOpenGL glx/egl context probe is exonerated (0.02 %); the cost was `renderShadowMaps` (**68 %** of frame) re-culling/re-drawing all 287 casters every frame with **no caching**. **Shipped R1** (cache caster world-points/AABB), **R2** (reuse spot/point depth maps while static), **R3** (cull once per directional light), **R4** (hoist instance grouping out of the per-cascade loop + vectorize modelview/buffer packing), **R5** (cache the grouping across frames/lights — reframed from a loader merge after profiling showed instancing already collapses the 287 casters to 29 draws). Walk-case shadow pass **22.4→6.2 ms (−72 %)**, spin **22.0→12.5 ms (−43 %)**, pixel-identical, 20 new TDD tests, 181-test regression sweep green. Remaining (future): vectorize `_cullOccluders`/`_extend_near_for_casters`; a real loader merge would help only the animated case |
| [RUNTIME-IBL.md](RUNTIME-IBL.md) | 🟡 Partial | **Per-object image-based lighting**: give *each object* its own irradiance + pre-filtered reflection maps computed from its own location (à la [diharaw/runtime-ibl](https://github.com/diharaw/runtime-ibl)) so it's lit by *what it can see* — the walls/floor and the scene's `Background` cube/sphere — not one global environment. **Cost model:** bake at **load time** for static geometry; **lazily re-bake** only the few objects that move a non-trivial distance, cached against a **down-sampled AABB centre** and regenerated only when the object drifts past that centre (not every frame, rate-limited). Capture per-object cubemap → irradiance convolution → GGX pre-filter mip chain + shared BRDF LUT → feed the existing `OPENGLCONTEXT_IBL=full` uniforms. **Radiance RGBE `.hdr` support shipped:** numpy-only RGBE decoder (`loaders/hdr.py`), CC0 Poly Haven HDRI catalogue (`loaders/hdri.py`), equirect→env-cube projection feeding the existing IBL probe (`ibl_equirect.frag`), an `HDRBackground` node (URL-loaded via the Resolver, ACES-tone-mapped equirect skybox, registers itself as the IBL radiance map), and `oglc-gltf --environment <name\|URL\|.hdr>` wiring — verified end-to-end on metal glTF spheres against Poly Haven studio/outdoor panoramas. Still planned: the per-object probe capture + load-time/lazy caching. Ties into [PBR-MATERIALS.md](PBR-MATERIALS.md) §9 IBL, [PBR-TEXTURE-LIBRARY.md](PBR-TEXTURE-LIBRARY.md) HDRIs, [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md) mover tracking, and GLTF EnvironmentTest parity |
| [OMI-ENVIRONMENT-SKY.md](OMI-ENVIRONMENT-SKY.md) | 📋 Planned | Load the [`OMI_environment_sky`](https://github.com/omigroup/gltf-extensions/tree/main/extensions/2.0/OMI_environment_sky) glTF extension and drive the scene's sky/environment from it. Its four sky types each map onto an existing background node — **gradient**→`SphereBackground`, **panorama** (equirect→`HDRBackground`+`ibl.set_equirect_env`, cubemap→`CubeBackground`), **plain**→`SimpleBackground` — so most of the work is reading the extension's `skies[]`/scene `sky` index (via the same `g.extensions`/`scene.extensions` seam as `KHR_lights_punctual`) into those nodes and registering the result as the IBL radiance source. Only the **physical** sky is new (analytic Rayleigh/Mie single-scattering skydome shader driven by the scene's directional sun, glTF inverse-metre units). Plus `ambientLightColor`/`ambientSkyContribution`→world-up ambient, `KHR_animation_pointer` on the colour/curve fields, `oglc-gltf` auto-pickup, and a per-type test + conformance sample. Ties into [RUNTIME-IBL.md](RUNTIME-IBL.md), [GLTF-FULL-FEATURE-CONFORMANCE.md](GLTF-FULL-FEATURE-CONFORMANCE.md), and the procedural-skydome todo |
| [PBR-TEXTURE-LIBRARY.md](PBR-TEXTURE-LIBRARY.md) | 📋 Planned | Import freely-licensed **CC0 PBR material sets** (ambientCG `.zip`s, Poly Haven, etc.) into ready-to-use `PBRMaterial` nodes so demos/samples stop hand-baking textures. Core work is format translation: CC0 ships separate Color/Roughness/Metalness/AO/Normal maps, glTF wants them **ORM-packed** (G=roughness, B=metallic, R=occlusion) with correct sRGB/linear + GL(+Y)-normal handling. Adds an import+cache+attach module (`loaders/material_library.py`), a bundled/fetchable CC0 material set with a licence-provenance manifest, and a spheres-in-a-row demo. Also surveys authored-*material* sources (glTF `KHR_materials_*`, **MaterialX**, HDRIs for IBL) as future standards-aligned imports. Ties into [PBR-MATERIALS.md](PBR-MATERIALS.md) + [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md) |
| [PBR-BRDF-CONFORMANCE.md](PBR-BRDF-CONFORMANCE.md) | ✅ Complete | Term-by-term audit of the PBR uber-shader **and all 19 supported glTF extensions** against Appendix B + each `KHR_*/EXT_*` spec + the Khronos reference renderer (6 parallel agent audits), **then the full punch-list was implemented**. **Core metallic-roughness BRDF is conformant** (D_GGX/height-correlated Smith V/Schlick F/Lambert identical to the reference; `α=roughness²`, 0.04 F0, `(1−F)(1−metallic)` energy split). Fixes landed (Tier 1/2/3): spotlight 2×-cone corrected (loader stores authored half-angles; shadow projection uses full-cone FOV); `node_visibility` propagates to children; sheen ported `V_Sheen`+`D_Charlie` with a 3rd BRDF-LUT channel for albedo-scaling and IBL sheen; specular carries `specularFactor` as f90 (direct+IBL+transmission) with specular/specularColor textures; iridescence `irSensitivity` takes a vec3 shift + `schlickToF0` kills the double-Fresnel; clearcoat uses the geometric/`clearcoatNormalTexture` coat normal; volume thickness scaled by model matrix + diffuse-transmission absorption tint; transmission `(1−specular)` energy term + IOR→mip + `KHR_materials_dispersion`; diffuse-transmission albedo double-tint removed, ambient de-energized, −N IBL back-glow; range-window applied once + spot falloff `t²`; `KHR_animation_pointer` implemented LIVE (Player-driven setters); `KHR_materials_anisotropy`; gpu-instancing normalized-int ROTATION dequantized; texture-transform `texCoord` override honoured. **Only deliberate exclusion:** `KHR_texture_basisu` (no Basis transcoder available; no demo model uses it, so unexercised). Overlapped P1/P6 in GLTF-FULL-FEATURE-CONFORMANCE + iridescence in GLTF-DEMO-CONFORMANCE |
| [PBR-MATERIALS.md](PBR-MATERIALS.md) | ✅ Complete | PBR metallic/roughness pass + `PBRMaterial` + glTF/GLB loader (URL streaming) + Khronos sample-model viewer; textures/normal/emissive + shadows. 16 tests. Proper metal rendering shipped (dual-mechanism IBL: `full` probe + corrected `analytic`, `OPENGLCONTEXT_IBL` switch + fps-adaptive degradation — §9). Per-object IBL is the open follow-on, tracked in [RUNTIME-IBL.md](RUNTIME-IBL.md) |
| [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md) | ✅ Complete | Close the gap to Khronos reference renders: catalogued from an 88-model QA pass, now **implemented** — animation, skinning, morph targets, real IBL (`OPENGLCONTEXT_IBL`), sampler/wrap+mipmaps, alpha blend + screen-space transmission, second UV `TEXCOORD_1`, sparse accessors, non-triangle primitives, computed tangents, `KHR_lights_punctual`, and the full `KHR_materials_*`/`KHR_texture_transform`/unlit/specGloss family (delivered with PBR-BRDF-CONFORMANCE). Background/async load + camera auto-framing landed. `KHR_draco_mesh_compression` now decodes via the optional `DracoPy` library (tracked in [DRACO-COMPRESSION.md](DRACO-COMPRESSION.md)) |
| [GLTF-FULL-FEATURE-CONFORMANCE.md](GLTF-FULL-FEATURE-CONFORMANCE.md) | 🟡 Partial | Render **all 121** GLB-shipping Khronos glTF-Sample-Assets models correctly (roster expanded from 23; migrated deprecated `glTF-Sample-Models`→`glTF-Sample-Assets` repo; `EXT_texture_webp` source fallback landed). Phased conformance push done: framing (VirtualCity/PotOfCoals/RecursiveSkeletons), **P1 punctual light photometric model** (inverse-square + `range` + candela/lux scale), P2 `KHR_materials_volume` thick-wall refraction (Glass\*/USDShaderBall), P3 background-at-infinity, P4 deterministic animation capture + `KHR_animation_pointer` + `KHR_node_visibility`, P5 `diffuse_transmission` + `dispersion`, P6 sheen/specular tint. Plus 2nd-pass `KHR_materials_anisotropy`, studio env, spotlight-cone fix + emissive bloom. **Still deferred (research-grade):** full volumetric subsurface (ScatteringSkull SSS) and nested-shell transmission (USDShaderBall) |
| [DRACO-COMPRESSION.md](DRACO-COMPRESSION.md) | ✅ Complete | Decode `KHR_draco_mesh_compression` glTF geometry using the optional `DracoPy` library when installed (soft dependency: `loaders/gltf/draco.py` decodes Draco only when present, warn-and-skip otherwise). Draco-compressed `.glb`/`.gltf` assets now load, verified against the Khronos Duck Draco variant (counts/bounds match the uncompressed model) with unit, missing-library, and render tests in `tests/test_gltf_draco.py`. Ties into [GLTF-COMPLETE-SUPPORT.md](GLTF-COMPLETE-SUPPORT.md) / [GLTF-FULL-FEATURE-CONFORMANCE.md](GLTF-FULL-FEATURE-CONFORMANCE.md) |
| [GLTF-REMOTE-RESOURCES.md](GLTF-REMOTE-RESOURCES.md) | ✅ Complete | The `oglc-gltf` viewer's URL path now goes through `load_gltf_url()` (resolver-backed), so remote multi-file `.gltf` documents resolve their external `.bin`/textures against the document origin; remote `.glb` unchanged; off-origin refs rejected. `_download` (single-file) removed; verified against Khronos `BoxTextured` + `TestRemoteSourceRouting` tests |
| [GLTF-DEMO-CONFORMANCE.md](GLTF-DEMO-CONFORMANCE.md) | ✅ Complete | QA/acceptance track for **100% reference parity** on the Khronos glTF-Sample-Models. **Feature/triage catalogue resolved** (iridescence, second UV, per-channel texture-transform, transmission/clearcoat textures, NegativeScale normals, emissive bloom/HDR, specular>1/EnvironmentTest env-cubemap IBL). **Acceptance layer delivered:** [tests/test_gltf_conformance.py](../../tests/test_gltf_conformance.py) gates **every** Khronos sample — roster expanded 122→**149 scenes** (all 148 catalogue models + Parthenon, 158 views), each rendered and diffed against a blessed baseline (`reference-images/gltf_baseline`) reusing `oglc-gltf-regression`'s `render_view`/`compare` + shared `ComparisonResult`; fast no-GL baseline-or-waived anchor + `slow`/`visual` render gate. **Two waived** (feature unimplemented): ScatteringSkull (subsurface) + USDShaderBallForGltf (nested-shell transmission) |
| [TESTSUITE-IMPROVEMENTS.md](TESTSUITE-IMPROVEMENTS.md) | 🟡 Partial | Automated pytest suite with subprocess isolation, visual regression, and HTML reports |
| Teapot UV coordinates | ✅ Complete | Per-vertex UV + PBR on the Teapot. Surfaced the GLU tessellator's parametric (u,v) via an opt-in `GL_MAP2_TEXTURE_COORD_2` map + texture-coord callback (`nurbs.py`), interleaved as **T2F_N3F_V3F** (texcoord→`aTexCoord` loc 0, not loc 2 as first assumed). Implements the Utah **"injective, with interior"** layout (graphics.cs.utah.edu/teapot): each Bézier patch maps to a non-overlapping atlas region, and each patch also emits reversed-winding, negated-normal **interior faces** into interior regions, rendered with **backface culling** (exterior outside / textured interior through the mouth, no z-fighting). Teapot also emits **tangents** (loc 3, `compute_tangents`) so the PBR normal/bump map works. **First PBR material on non-glTF geometry:** `tests/teapot_ceramic.py` applies a `PBRMaterial` (procedural celadon glaze + *arare* bump, `tests/_ceramic_textures.py`) to the plain `Teapot` Shape — no `Shape` change needed (its existing PBR branch drives it once the geometry supplies texcoords/tangents). Works in core + compat profiles. New injective-layout/interior + non-glTF-PBR tests (`test_teapot_nurbs.py`, `test_pbr_rendering.py`). Caveat: injective layout is ~10× anisotropic on the body, so the bump is kept shallow (reads as throwing-rings there) |
| Godot scene loading | ⬜ Todo | Load Godot scene descriptions (`.tscn`/`.escn`, and/or `.scn`) into the scenegraph — parse the node tree, transforms, meshes and materials into OpenGLContext nodes |
| Quake 3 BSP loading | ⬜ Todo | Load Quake 3 style BSP maps, reusing the BSP parsing/rendering code in the `twitch/` directory of this workspace — lightmaps, PVS/cluster visibility, brush geometry |
| Retire render-visitor passes | ⬜ Todo | Remove the old render-visitor pass machinery and standardize on the flat passes (`_flat.py`/`flatcore.py`/`flatcompat.py`) as the only rendering path |
| [TERRAIN-SYSTEM.md](TERRAIN-SYSTEM.md) | 📋 Planned | Production-grade large-world terrain + vegetation. **Standards-first, 3D-Tiles-native**: an OGC **3D Tiles** streaming runtime (`tileset.json` quadtree/octree BVH + screen-space-error LOD + async paging + LRU eviction) where **surface, caves, overhangs and props are all glTF mesh tiles** in one octree (rendered by the existing conformant glTF loader). Runtime ElevationGrid/GPU-tessellation dropped (can't displace a baked mesh); heightfields/DEMs are an *offline* bake input only. Costs paid explicitly: discrete-LOD popping → **geomorphing**, mesh weight → **Draco/meshopt** compression ([DRACO-COMPRESSION.md](DRACO-COMPRESSION.md)). **Caves** = glTF octree tiles, authored or **Transvoxel**-baked from `heightfield_sdf − Σ cave_brushes` (compute on 3060, CPU+cache elsewhere) → fissures/crevasses/overhangs. One **triplanar PBR** splat material reusing CSM/IBL; per-tile physics `trimesh` colliders (walkable caves). **Vegetation**: instanced glTF via `EXT_mesh_gpu_instancing` + octahedral impostors (trees/brush), compute-scatter + `glDrawElementsIndirect` GPU grass/flowers (VolumetricTree rejected as too slow). Capability tiers: RTX 3060 primary, modern iGPU, GL 3.3/software floor (surface path identical across tiers) |
| [TERRAIN-ASSET-FORMATS.md](TERRAIN-ASSET-FORMATS.md) | ✅ Complete | Reference: input asset formats for `SplatTerrain`/`HeightField` + provenance of the forest demo's terrain. Documents (1) the **heightmap** — a 16-bit grayscale PNG made from **AWS Terrain Tiles (Terrarium)** for the Great Smoky Mountains (lat 35.60, lon −83.50, z12, 4×4→1024²) via `elev = R·256+G+B/256−32768`, normalised; loaded by `HeightField.from_image` (`/65535 → 0..1`, ×`relief` over an `extent` square, world↔texel mapping); (2) the **RGBA splat control map** — one channel per detail layer (R=forest_floor,G=grass,B=rock,A=moss), sampled by world XZ and normalised in `terrain_splat.frag`, generated from the DEM's slope+elevation; (3) the CC0 detail-layer material arrays. Includes the complete runnable fetch/decode + control-build recipes (were ad-hoc scripts, not a committed tool) |
| Procedural backgrounds / environment | ⬜ Todo | Replace the flat, artificial-looking demo backgrounds with lively procedural environments: fractal/volumetric clouds (FBM noise skydome or raymarched), choppy animated water (Gerstner waves / FFT ocean with reflections), and procedurally generated ground/terrain (noise heightfield with texture-splatting). Goal is demos that feel like a real scene instead of a plain gradient sky + empty floor. Ties into [ROAM / modern terrain] for the ground and [ORDER-INDEPENDENT-TRANSPARENCY.md](ORDER-INDEPENDENT-TRANSPARENCY.md)/water reflections |
| Void gravity suppression | 🟡 Partial | Character hovers instead of dropping when no geometry lies below it. Opt-in `CharacterCapabilities.suppressOverVoid` (default off); `CharacterController._over_void()` casts five infinite straight-down rays (capsule base centre + four radius-ring points) against static-collider **AABBs** — a void is where *every* ray misses, so a footprint still partly on ground falls/slides off normally. The gravity step is gated on `vy <= 0` so a jump *across* a void still arcs up. AABB-level only (won't see holes inside one collider's bounding box; errs toward "fall" when unsure). No new raycast/BVH — reuses the controller's cached static proxies. `tests/test_character_void.py` (7 tests). Interactive `physics_void.py` demo still TODO. See [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md) |
| [PHYSICS-COLLISION.md](PHYSICS-COLLISION.md) | ✅ Complete (all 8 phases, 0–7) | Real-time game-like rigid-body physics beside the scenegraph, **built natively on the OMI glTF physics model** (`OMI_physics_shape`/`_body`/`_gravity`/`_joint`). **Shipped** as the `OpenGLContext/physics/` package (model/world/backend/body/collide/gjk/broadphase/narrowphase/solver/hull/cookery/gravity/triggers/joints/character/omi_gltf/debugdraw/manager/demo) + `scenegraph/physicsbody.py` + `move/physicsplatform.py` + `bin/physics_cook.py`. Symplectic-Euler + fixed-timestep accumulator + render interpolation, global+volume gravity (priority/replace/stop), linear+quadratic drag, materials (friction/restitution + combine modes), collision filters, dynamic AABB-tree broad phase, analytic + **GJK/EPA** narrow phase, sequential-impulse contact+joint solver (warm-start + split-impulse position correction, contact **islands**, **sleeping**), triggers (enter/stay/exit, no impulse). Mesh↔mesh via OMI `convex` (own QuickHull + approximate convex decomposition, no scipy) + static `trimesh` triangle-soup; **cooking** (`cook_shape`) + `physics-cook` bake CLI. `PhysicsBody` syncs `Transform`; `PhysicsViewPlatform` (non-OMI `CharacterCapabilities`: walk/run/sprint/crouch/jump/fly, step/slope) with **safe viewpoint binding** (depenetration + ground-snap, never stuck). SoA columnar layout + kernel-shaped stages + `backend.py` seam are **GPGPU-accelerated**: `GLComputeBackend` (`glcompute.py`) runs per-body force/position integration as **GL 4.3 compute shaders** (fused single dispatch for collision-free steps), ~2.4× over numpy at 10⁵ movers; the default `auto` policy stays on numpy below `gpu_threshold` (10k) awake bodies and hands off to the GPU above it (hysteresis + numpy fallback; broad/narrow/solve stay CPU; full GPU-resident loop future). **Debug overlay** renders proxies/AABBs/contacts + per-body **velocity/acceleration/angular-velocity** (corner-spin) vectors, sleep state and joint links. **Red/Green TDD throughout: 91 tests green** across 18 modules; **9 interactive demos** (`tests/physics_*.py`) double as visual-regression tests (verified via the auto-exit/capture harness); `docs/physics.html` + getting-started tutorial, linked from the docs index. Scale = import-time only |

## Recent Progress (January 2026)

### Test Suite Implementation

The automated test suite is now functional with the following capabilities:

- **118 visual test scripts** run with screenshot capture and comparison
- **129 total test scripts** including non-visual functionality tests
- **HTML report generation** at `tests/report.html` with side-by-side image comparisons
- **Coverage collection** from subprocess test runs (currently ~9% coverage)
- **Auto-exit mechanism** via `OPENGLCONTEXT_AUTO_EXIT_FRAMES` environment variable
- **Screenshot capture** via `OPENGLCONTEXT_AUTO_EXIT_CAPTURE_DIR` and `OPENGLCONTEXT_AUTO_EXIT_CAPTURE_NAME`
- **FPS display toggle** via `OPENGLCONTEXT_DISABLE_FPS_DISPLAY` for clean screenshots

Script categorization:

- Platform-specific scripts (Windows/WGL, wxPython, pygame) with automatic skip logic
- Non-visual scripts (glget.py, boundingvolume.py, etc.) excluded from visual regression
- Randomized scripts (particles, starfield) marked with `expect_visual_diff`

Test results from last run: 247 passed, 7 failed, 6 skipped

### Known Issues

- `solid_font.py` - Font provider registry issue (`fontNameFromStyle` on None)
- `glutmousewheel.py` - GLUT-specific test failing
- `savepostscript.py` - PostScript save functionality
- `shader_instanced*.py` - Legacy GLSL compatibility issues on some drivers
